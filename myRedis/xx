cd src && make all
# make[1]: 进入目录“/usr/local/gitcode/myLinuxFile/myRedis/src”
# GNU Make 4.2.1
# 为 x86_64-pc-linux-gnu 编译
# Copyright (C) 1988-2016 Free Software Foundation, Inc.
# 许可证：GPLv3+：GNU 通用公共许可证第 3 版或更新版本<http://gnu.org/licenses/gpl.html>。
# 本软件是自由软件：您可以自由修改和重新发布它。
# 在法律允许的范围内没有其他保证。

# make 数据基础，打印在 Wed Mar 15 10:42:41 2023

# 变量

# 环境
VSCODE_GIT_ASKPASS_MAIN = /usr/share/code/resources/app/extensions/git/dist/askpass-main.js
# “override”指令
GNUMAKEFLAGS := 
# makefile (从“Makefile”，行 228)
LIBSYSTEMD_LIBS = -lsystemd
# 自动
<D = $(patsubst %/,%,$(dir $<))
# 自动
?F = $(notdir $?)
# 环境
DESKTOP_SESSION = ubuntu
# 默认
.SHELLFLAGS := -c
# 环境
XDG_SESSION_CLASS = user
# makefile (从“Makefile”，行 266)
FINAL_LIBS := ../deps/jemalloc/lib/libjemalloc.a -lm -ldl -pthread -lrt
# makefile (从“Makefile”，行 330)
QUIET_INSTALL = @printf '    %b %b\n' $(LINKCOLOR)INSTALL$(ENDCOLOR) $(BINCOLOR)$@$(ENDCOLOR) 1>&2;
# makefile (从“Makefile”，行 344)
REDIS_CHECK_RDB_NAME = redis-check-rdb$(PROG_SUFFIX)
# 环境
XDG_SESSION_TYPE = x11
# 默认
CWEAVE = cweave
# 环境
NVM_INC = /home/michael/.nvm/versions/node/v17.9.0/include/node
# 自动
?D = $(patsubst %/,%,$(dir $?))
# 环境
IM_CONFIG_PHASE = 1
# makefile (从“Makefile”，行 55)
INSTALL = install
# 自动
@D = $(patsubst %/,%,$(dir $@))
# 环境
XAUTHORITY = /run/user/1000/gdm/Xauthority
# 环境
GDMSESSION = ubuntu
# 环境
XMODIFIERS = @im=ibus
# makefile
CURDIR := /usr/local/gitcode/myLinuxFile/myRedis/src
# makefile
SHELL = /bin/sh
# 默认
RM = rm -f
# 默认
CO = co
# 环境
JOURNAL_STREAM = 8:51226
# makefile (从“Makefile”，行 18)
OPTIMIZATION = -O0
# 默认
PREPROCESS.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -F
# 默认
LINK.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
XDG_CONFIG_DIRS = /etc/xdg/xdg-ubuntu:/etc/xdg
# 默认
OUTPUT_OPTION = -o $@
# makefile (从“Makefile”，行 295)
TLS_MODULE_NAME := redis-tls.so
# makefile (从“Makefile”，行 287)
BUILD_MODULE := 2
# makefile (从“.make-settings”，行 11)
PREV_FINAL_CFLAGS = -pedantic -DREDIS_STATIC= -std=c11 -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations -O3 -g -ggdb -flto -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# 默认
COMPILE.cpp = $(COMPILE.cc)
# makefile (从“.make-settings”，行 1)
MAKEFILE_LIST :=  Makefile .make-settings
# makefile (从“Makefile”，行 24)
NODEPS := clean distclean
# 自动
@F = $(notdir $@)
# makefile (从“Makefile”，行 305)
define MAKE_INSTALL
        @printf '    %b %b\n' $(LINKCOLOR)INSTALL$(ENDCOLOR) $(BINCOLOR)$(1)$(ENDCOLOR) 1>&2
        @$(INSTALL) $(1) $(2)
endef
# 环境
GIO_LAUNCHED_DESKTOP_FILE = /usr/share/applications/code.desktop
# makefile (从“Makefile”，行 117)
DEBUG = -g -ggdb
# 环境
USERNAME = michael
# 默认
LINK.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
MAKELEVEL := 1
# 环境
XDG_DATA_DIRS = /usr/share/ubuntu:/usr/local/share/:/usr/share/:/var/lib/snapd/desktop
# makefile (从“Makefile”，行 296)
TLS_MODULE_CFLAGS := -pedantic -DREDIS_STATIC= -std=c11 -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations -O3 -g -ggdb  -flto -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# 环境
DBUS_SESSION_BUS_ADDRESS = unix:path=/run/user/1000/bus
# 默认
CC = cc
# 环境
_ = /usr/bin/make
# makefile (从“Makefile”，行 56)
PKG_CONFIG = pkg-config
# 默认
CHECKOUT,v = +$(if $(wildcard $@),,$(CO) $(COFLAGS) $< $@)
# 环境
MANAGERPID = 1574
# makefile (从“Makefile”，行 233)
LIBSYSTEMD_PKGCONFIG := 
# 环境
LESSOPEN = | /usr/bin/lesspipe %s
# makefile (从“.make-settings”，行 6)
USE_SYSTEMD = 
# 环境
LC_NAME = zh_CN.UTF-8
# 默认
CPP = $(CC) -E
# 默认
LINK.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
MAKE_HOST := x86_64-pc-linux-gnu
# makefile (从“Makefile”，行 345)
REDIS_CHECK_AOF_NAME = redis-check-aof$(PROG_SUFFIX)
# 环境
PATH = /home/michael/.nvm/versions/node/v17.9.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
# 默认
LD = ld
# 默认
TEXI2DVI = texi2dvi
# 环境
GPG_AGENT_INFO = /run/user/1000/gnupg/S.gpg-agent:0:1
# 默认
YACC = yacc
# 环境
SESSION_MANAGER = local/michael-CW65S:@/tmp/.ICE-unix/1776,unix/michael-CW65S:/tmp/.ICE-unix/1776
# makefile (从“Makefile”，行 336)
REDIS_SERVER_OBJ = server.o setproctitle.o zmalloc.o crcspeed.o crc64.o ./Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
# makefile (从“Makefile”，行 280)
LIBCRYPTO_LIBS = $(shell $(PKG_CONFIG) --libs libcrypto)
# 默认
COMPILE.mod = $(M2C) $(M2FLAGS) $(MODFLAGS) $(TARGET_ARCH)
# 环境
XDG_RUNTIME_DIR = /run/user/1000
# makefile (从“.make-settings”，行 7)
CFLAGS = 
# 默认
ARFLAGS = rv
# 默认
LINK.r = $(FC) $(FFLAGS) $(RFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
LINT = lint
# 默认
COMPILE.f = $(FC) $(FFLAGS) $(TARGET_ARCH) -c
# 默认
LINT.c = $(LINT) $(LINTFLAGS) $(CPPFLAGS) $(TARGET_ARCH)
# 默认
YACC.m = $(YACC) $(YFLAGS)
# 环境
NVM_BIN = /home/michael/.nvm/versions/node/v17.9.0/bin
# 默认
YACC.y = $(YACC) $(YFLAGS)
# makefile (从“Makefile”，行 273)
LIBSSL_LIBS = $(shell $(PKG_CONFIG) --libs libssl)
# makefile (从“Makefile”，行 264)
DEPENDENCY_TARGETS = hiredis linenoise lua hdr_histogram fpconv jemalloc
# 默认
AR = ar
# 环境
XDG_MENU_PREFIX = gnome-
# 环境
LC_NUMERIC = zh_CN.UTF-8
# 环境
INVOCATION_ID = ecb31a0f7d0042b2b3f1373d3de7451c
# makefile (从“.make-settings”，行 8)
LDFLAGS = 
# 默认
.FEATURES := target-specific order-only second-expansion else-if shortest-stem undefine oneshell archives jobserver output-sync check-symlink load
# 默认
TANGLE = tangle
# makefile (从“Makefile”，行 15)
release_hdr := #define REDIS_GIT_SHA1 "f69f6aac" #define REDIS_GIT_DIRTY "27"
# 环境
LS_COLORS = rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=30;41:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:
# 环境
XDG_SESSION_DESKTOP = ubuntu
# 环境
SSH_AUTH_SOCK = /run/user/1000/keyring/ssh
# 环境
GIO_LAUNCHED_DESKTOP_FILE_PID = 4959
# 默认
GET = get
# makefile (从“Makefile”，行 320)
LINKCOLOR = "\033[34;1m"
# 自动
%F = $(notdir $%)
# 环境
DISPLAY = :0
# 默认
COMPILE.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“Makefile”，行 227)
BUILD_WITH_SYSTEMD = no
# makefile (从“Makefile”，行 294)
TLS_MODULE = 
# 默认
CTANGLE = ctangle
# makefile (从“Makefile”，行 43)
NUMBER_SIGN_CHAR := #
# makefile (从“.make-settings”，行 3)
OPT = -O3
# 默认
.LIBPATTERNS = lib%.so lib%.a
# makefile (从“Makefile”，行 323)
MAKECOLOR = "\033[32;1m"
# 默认
LINK.C = $(LINK.cc)
# 环境
PWD = /usr/local/gitcode/myLinuxFile/myRedis/src
# makefile (从“Makefile”，行 319)
CCCOLOR = "\033[34m"
# 默认
.LOADED := 
# 默认
LINK.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 默认
PREPROCESS.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -F
# 环境
SSH_AGENT_PID = 1746
# 环境
ORIGINAL_XDG_CURRENT_DESKTOP = ubuntu:GNOME
# 环境
GTK_MODULES = gail:atk-bridge
# makefile (从“Makefile”，行 316)
REDIS_LD = $(QUIET_LINK)$(CC) $(FINAL_LDFLAGS)
# makefile (从“Makefile”，行 59)
PYTHON := /usr/bin/python3
# 默认
LINK.s = $(CC) $(ASFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 环境
HOME = /home/michael
# 环境
LESSCLOSE = /usr/bin/lesspipe %s %s
# makefile (从“Makefile”，行 278)
LIBCRYPTO_PKGCONFIG := 0
# makefile (从“Makefile”，行 203)
FINAL_LDFLAGS = $(LDFLAGS) $(REDIS_LDFLAGS) $(DEBUG) -rdynamic
# makefile (从“.make-settings”，行 2)
WARN = -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations
# 环境
LOGNAME = michael
# 环境
NVM_CD_FLAGS = 
# 自动
+F = $(notdir $+)
# makefile (从“Makefile”，行 54)
INSTALL_BIN = $(PREFIX)/bin
# makefile (从“Makefile”，行 53)
PREFIX = /usr/local
# makefile (从“Makefile”，行 333)
REDIS_SERVER_NAME = redis-server$(PROG_SUFFIX)
# 环境
GNOME_DESKTOP_SESSION_ID = this-is-deprecated
# 自动
^D = $(patsubst %/,%,$(dir $^))
# makefile (从“Makefile”，行 271)
LIBSSL_PKGCONFIG := 0
# makefile (从“Makefile”，行 315)
REDIS_CC = $(QUIET_CC)$(CC) $(FINAL_CFLAGS)
# 环境
LC_TIME = zh_CN.UTF-8
# 环境
VSCODE_GIT_IPC_HANDLE = /run/user/1000/vscode-git-6c0f1317c6.sock
# 默认
COMPILE.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“.make-settings”，行 10)
REDIS_LDFLAGS = -flto
# 环境
COLORTERM = truecolor
# makefile (从“Makefile”，行 328)
QUIET_GEN = @printf '    %b %b\n' $(CCCOLOR)GEN$(ENDCOLOR) $(SRCCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
MAKE = $(MAKE_COMMAND)
# makefile (从“Makefile”，行 44)
C11_ATOMIC := yes
# 默认
MAKECMDGOALS := all
# 环境
QT_IM_MODULE = ibus
# 环境
SHLVL = 1
# makefile (从“Makefile”，行 338)
REDIS_CLI_NAME = redis-cli$(PROG_SUFFIX)
# 默认
AS = as
# 默认
PREPROCESS.S = $(CC) -E $(CPPFLAGS)
# 环境
GNOME_SHELL_SESSION_MODE = ubuntu
# 默认
COMPILE.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 默认
MAKE_VERSION := 4.2.1
# 环境
USER = michael
# makefile (从“Makefile”，行 327)
QUIET_CC = @printf '    %b %b\n' $(CCCOLOR)CC$(ENDCOLOR) $(SRCCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
FC = f77
# 环境
LC_MONETARY = zh_CN.UTF-8
# makefile
.DEFAULT_GOAL := all
# 环境
XDG_CURRENT_DESKTOP = Unity
# makefile (从“.make-settings”，行 12)
PREV_FINAL_LDFLAGS = -flto -g -ggdb -rdynamic
# makefile (从“Makefile”，行 340)
REDIS_CLI_OBJ = redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o anet.o ae.o siphash.o crc64.o strl.o cli_common.o monotonic.o crcspeed.o redisassert.o
# 自动
%D = $(patsubst %/,%,$(dir $%))
# 环境
LC_TELEPHONE = zh_CN.UTF-8
# makefile (从“Makefile”，行 286)
BUILD_YES := 1
# makefile (从“.make-settings”，行 9)
REDIS_CFLAGS = -flto
# 默认
WEAVE = weave
# 默认
MAKE_COMMAND := make
# 默认
LINK.cpp = $(LINK.cc)
# makefile (从“.make-settings”，行 1)
STD = -pedantic -DREDIS_STATIC= -std=c11
# 默认
F77 = $(FC)
# 环境
OLDPWD = /usr/local/gitcode/myLinuxFile/myRedis
# 环境
VSCODE_GIT_ASKPASS_EXTRA_ARGS = --ms-enable-electron-run-as-node
# 环境
TERM_PROGRAM = vscode
# 默认
.VARIABLES := 
# 默认
PC = pc
# 自动
*F = $(notdir $*)
# 环境
LANGUAGE = zh_CN:zh:en_US:en
# makefile (从“Makefile”，行 17)
uname_M := x86_64
# makefile (从“Makefile”，行 324)
ENDCOLOR = "\033[0m"
# 环境
VSCODE_GIT_ASKPASS_NODE = /usr/share/code/code
# 默认
COMPILE.def = $(M2C) $(M2FLAGS) $(DEFFLAGS) $(TARGET_ARCH)
# 默认
LEX = lex
# makefile
MAKEFLAGS = pw
# 环境
MFLAGS = -pw
# 自动
*D = $(patsubst %/,%,$(dir $*))
# 环境
TERM_PROGRAM_VERSION = 1.76.0
# 环境
NVM_DIR = /home/michael/.nvm
# 默认
LEX.l = $(LEX) $(LFLAGS) -t
# 默认
LEX.m = $(LEX) $(LFLAGS) -t
# 自动
+D = $(patsubst %/,%,$(dir $+))
# 默认
COMPILE.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -c
# 环境
GDK_BACKEND = x11
# 环境
Dotnet_Dir = /home/michael/.dotnet/tools
# 默认
M2C = m2c
# 环境
LC_IDENTIFICATION = zh_CN.UTF-8
# 环境
QT_ACCESSIBILITY = 1
# makefile (从“Makefile”，行 343)
REDIS_BENCHMARK_OBJ = server.o
# “override”指令
.SHELLSTATUS := 0
# 环境
GIT_ASKPASS = /usr/share/code/resources/app/extensions/git/dist/askpass.sh
# 默认
MAKEFILES := 
# makefile (从“Makefile”，行 421)
DEP = $(REDIS_SERVER_OBJ:%.o=%.d) $(REDIS_CLI_OBJ:%.o=%.d) $(REDIS_BENCHMARK_OBJ:%.o=%.d)
# 默认
COMPILE.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 自动
<F = $(notdir $<)
# makefile (从“Makefile”，行 285)
BUILD_NO := 0
# makefile (从“Makefile”，行 334)
REDIS_SENTINEL_NAME = redis-sentinel$(PROG_SUFFIX)
# 自动
^F = $(notdir $^)
# makefile (从“.make-settings”，行 4)
MALLOC = jemalloc
# 默认
CXX = g++
# 环境
LC_MEASUREMENT = zh_CN.UTF-8
# makefile (从“Makefile”，行 341)
REDIS_BENCHMARK_NAME = redis-benchmark$(PROG_SUFFIX)
# 默认
COFLAGS = 
# makefile (从“Makefile”，行 317)
REDIS_INSTALL = $(QUIET_INSTALL)$(INSTALL)
# makefile (从“Makefile”，行 16)
uname_S := Linux
# 默认
COMPILE.C = $(COMPILE.cc)
# makefile (从“Makefile”，行 321)
SRCCOLOR = "\033[33m"
# 环境
GJS_DEBUG_TOPICS = JS ERROR;JS LOG
# 环境
LC_PAPER = zh_CN.UTF-8
# 默认
COMPILE.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(TARGET_MACH) -c
# 默认
LINK.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# makefile (从“Makefile”，行 346)
ALL_SOURCES = $(sort $(patsubst %.o,%.c,$(REDIS_SERVER_OBJ) $(REDIS_CLI_OBJ) $(REDIS_BENCHMARK_OBJ)))
# makefile (从“Makefile”，行 329)
QUIET_LINK = @printf '    %b %b\n' $(LINKCOLOR)LINK$(ENDCOLOR) $(BINCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
SUFFIXES := .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# 默认
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“.make-settings”，行 5)
BUILD_TLS = 
# 环境
CHROME_DESKTOP = code-url-handler.desktop
# 默认
LINK.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
WINDOWPATH = 2
# 默认
COMPILE.s = $(AS) $(ASFLAGS) $(TARGET_MACH)
# 默认
.INCLUDE_DIRS = /usr/include /usr/local/include /usr/include
# 环境
GJS_DEBUG_OUTPUT = stderr
# 默认
.RECIPEPREFIX := 
# 默认
MAKEINFO = makeinfo
# 环境
MAKE_TERMERR = /dev/pts/2
# 默认
OBJC = cc
# 环境
LC_ADDRESS = zh_CN.UTF-8
# 默认
LINK.f = $(FC) $(FFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
TEX = tex
# 环境
LANG = zh_CN.UTF-8
# 环境
TERM = xterm-256color
# 默认
F77FLAGS = $(FFLAGS)
# 默认
LINK.o = $(CC) $(LDFLAGS) $(TARGET_ARCH)
# makefile (从“Makefile”，行 265)
FINAL_CFLAGS = $(STD) $(WARN) $(OPT) $(DEBUG) $(CFLAGS) $(REDIS_CFLAGS) -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# makefile (从“Makefile”，行 322)
BINCOLOR = "\033[37;1m"
# 变量的杂凑表状态:
# 负载=239/1024=23%, 重新杂凑=0, 碰撞=101/463=22%

# Pattern-specific 变量值

# 没有 pattern-specific 变量的值。

# 目录

# Main (设备 2053，i-节点 6031162)： 5 文件， 19 不可能.
# SCCS：无法对其进行 stat 操作。
# Main/SCCS：无法对其进行 stat 操作。
# . (设备 2053，i-节点 6030306)： 86 文件， 684 不可能.
# commands (设备 2053，i-节点 6171219)： 4 文件， 无 不可能.
# Main/RCS：无法对其进行 stat 操作。
# RCS：无法对其进行 stat 操作。

# 95 文件， 703 不可能在 7 目录中。

# 隐含规则

%.o: %.c .make-prerequisites
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

%.out:

%.a:

%.ln:

%.o:

%: %.o
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.c:

%: %.c
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.ln: %.c
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

%.o: %.c
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

%.cc:

%: %.cc
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cc
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

%.C:

%: %.C
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.C
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

%.cpp:

%: %.cpp
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cpp
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

%.p:

%: %.p
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.p
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

%.f:

%: %.f
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.f
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

%.F:

%: %.F
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.F
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

%.f: %.F
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

%.m:

%: %.m
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.m
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

%.r:

%: %.r
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.r
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

%.f: %.r
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

%.y:

%.ln: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

%.c: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

%.l:

%.ln: %.l
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

%.c: %.l
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

%.r: %.l
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

%.ym:

%.m: %.ym
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

%.yl:

%.s:

%: %.s
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.s
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

%.S:

%: %.S
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.S
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

%.s: %.S
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

%.mod:

%: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

%.o: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

%.sym:

%.def:

%.sym: %.def
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

%.h:

%.info:

%.dvi:

%.tex:

%.dvi: %.tex
#  要执行的配方 (内置)：
	$(TEX) $<

%.texinfo:

%.info: %.texinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.texi:

%.info: %.texi
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texi
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.txinfo:

%.info: %.txinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.txinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.w:

%.c: %.w
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

%.tex: %.w
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

%.ch:

%.web:

%.p: %.web
#  要执行的配方 (内置)：
	$(TANGLE) $<

%.tex: %.web
#  要执行的配方 (内置)：
	$(WEAVE) $<

%.sh:

%: %.sh
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

%.elc:

%.el:

(%): %
#  要执行的配方 (内置)：
	$(AR) $(ARFLAGS) $@ $<

%.out: %
#  要执行的配方 (内置)：
	@rm -f $@ 
	 cp $< $@

%.c: %.w %.ch
#  要执行的配方 (内置)：
	$(CTANGLE) $^ $@

%.tex: %.w %.ch
#  要执行的配方 (内置)：
	$(CWEAVE) $^ $@

%:: %,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

%:: SCCS/s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

# 93 条隐含规则，5 (5.4%) 终端。

# 文件

# 不是一个目标：
server.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
networking.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.web.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TANGLE) $<

# 不是一个目标：
sha256.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.l.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

# 不是一个目标：
unix.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.ym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
../utils/generate-command-code.py:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.f.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

all: redis-server redis-cli
#  假目标 (.PHONY的前提)。
#  命令行目标。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 350)：
	@echo ""
	@echo "Hint: It's a good idea to run 'make test' ;)"
	@echo ""

# 不是一个目标：
.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.y:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.def.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

# 不是一个目标：
anet.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
crc64.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
redis-cli.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
siphash.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

.PHONY: all persist-settings clean distclean lcov
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
ae.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

32bit:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 477)：
	@echo ""
	@echo "WARNING: if it fails under Linux you probably need to install libc6-dev-i386"
	@echo ""
	$(MAKE) CFLAGS="-m32" LDFLAGS="-m32"

# 不是一个目标：
.p.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

# 不是一个目标：
.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.txinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
Main/mt19937-64.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redis-sentinel: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 399)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_SENTINEL_NAME)

commands.c: commands/ping.json commands/client.json ../utils/generate-command-code.py
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 434)：
	$(QUIET_GEN)$(PYTHON) ../utils/generate-command-code.py

# 不是一个目标：
dict.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.a:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
zmalloc.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
object.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.yl:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

# 不是一个目标：
.F.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

# 不是一个目标：
multi.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.texi.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.w.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

# 不是一个目标：
.texi.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.ch:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

distclean: clean
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 444)：
	-(cd ../deps && $(MAKE) distclean)
	-(cd modules && $(MAKE) clean)
	-(cd ../tests/modules && $(MAKE) clean)
	-(rm -f .make-*)

# 不是一个目标：
crcspeed.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.m.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

redis-check-aof: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 407)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_CHECK_AOF_NAME)

# 不是一个目标：
.cc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

Makefile.dep:
#  对隐含规则的搜索尚未完成。
#  隐含/静态模式主干：“”
#  最近更新 2023-03-15 10:42:41.258981025
#  文件已经被更新。
#  更新成功。
# 自动
# @ := Makefile.dep
# 自动
# % := 
# 自动
# * := 
# 自动
# + := 
# 自动
# | := 
# 自动
# < := 
# 自动
# ^ := 
# 自动
# ? := 
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=6/30=20%
#  要执行的配方 (从“Makefile”，行 355)：
	-$(REDIS_CC) -MM $(ALL_SOURCES) > Makefile.dep 2> /dev/null || true

# 不是一个目标：
.cc.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

# 不是一个目标：
.def:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.SUFFIXES: .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
localtime.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
strl.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
adlist.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

.make-prerequisites:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 382)：
	@touch $@

# 不是一个目标：
.c.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

# 不是一个目标：
Makefile:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-15 10:14:33.54794888
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.r.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

helgrind:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 492)：
	$(MAKE) OPTIMIZATION="-O0" MALLOC="libc" CFLAGS="-D__ATOMIC_VAR_FORCE_SYNC_MACROS" REDIS_CFLAGS="-I/usr/local/include" REDIS_LDFLAGS="-L/usr/local/lib"

# 不是一个目标：
server.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
networking.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
sha256.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

test: redis-server redis-check-aof redis-cli redis-benchmark
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 452)：
	@(cd ..; ./runtest)

# 不是一个目标：
.ym.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
.y.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

test-modules: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 455)：
	@(cd ..; ./runtest-moduleapi)

# 不是一个目标：
unix.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
monotonic.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.elc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

# 不是一个目标：
Main/mt19937-64.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
sds.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.out:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redis-cli: redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o anet.o ae.o siphash.o crc64.o strl.o cli_common.o monotonic.o crcspeed.o redisassert.o
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 415)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/linenoise/linenoise.o $(FINAL_LIBS) $(TLS_CLIENT_LIBS)

# 不是一个目标：
crc64.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

test-sentinel: redis-sentinel redis-cli
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 458)：
	@(cd ..; ./runtest-sentinel)

# 不是一个目标：
redis-cli.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
siphash.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.C:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
redisassert.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.r.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
.S:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

lcov:
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 466)：
	$(MAKE) gcov
	@(set -e; cd ..; ./runtest --clients 1)
	@geninfo -o redis.info .
	@genhtml --legend -o lcov-html redis.info

# 不是一个目标：
commands/ping.json:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
acl.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

valgrind:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 489)：
	$(MAKE) OPTIMIZATION="-O0" MALLOC="libc"

# 不是一个目标：
.texinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.w.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

# 不是一个目标：
.c.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

# 不是一个目标：
.s.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

# 不是一个目标：
.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
endianconv.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
dict.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
setproctitle.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
tracking.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

redis-benchmark: server.o
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 419)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/hdr_histogram/libhdrhistogram.a $(FINAL_LIBS) $(TLS_CLIENT_LIBS)

# 不是一个目标：
crcspeed.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
commands.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
commands/client.json:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.texinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.el:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redis-check-rdb: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 403)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_CHECK_RDB_NAME)

uninstall:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 504)：
	rm -f $(INSTALL_BIN)/{$(REDIS_SERVER_NAME),$(REDIS_BENCHMARK_NAME),$(REDIS_CLI_NAME),$(REDIS_CHECK_RDB_NAME),$(REDIS_CHECK_AOF_NAME),$(REDIS_SENTINEL_NAME)}

# 不是一个目标：
.lm.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.m) $< > $@

# 不是一个目标：
cli_common.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.make-settings:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-11 21:44:31.213406742
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.y.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
t_stream.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.web.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(WEAVE) $<

# 不是一个目标：
.texinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
connection.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.DEFAULT:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.h:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
socket.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

redis-server: server.o setproctitle.o zmalloc.o crcspeed.o crc64.o Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 395)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/lua/src/liblua.a ../deps/hdr_histogram/libhdrhistogram.a ../deps/fpconv/libfpconv.a $(FINAL_LIBS)

test-cluster: redis-server redis-cli
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 461)：
	@(cd ..; ./runtest-cluster)

# 不是一个目标：
util.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
tls.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
monotonic.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.tex.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEX) $<

# 不是一个目标：
.cpp.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

# 不是一个目标：
.cpp:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
syncio.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

clean:
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 438)：
	rm -rf $(REDIS_SERVER_NAME) $(REDIS_SENTINEL_NAME) $(REDIS_CLI_NAME) $(REDIS_BENCHMARK_NAME) $(REDIS_CHECK_RDB_NAME) $(REDIS_CHECK_AOF_NAME) *.o *.gcda *.gcno *.gcov redis.info lcov-html Makefile.dep *.so
	rm -f $(DEP)

# 不是一个目标：
.C.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

# 不是一个目标：
sds.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.texi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.txinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
redisassert.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.txinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
endianconv.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

bench: redis-benchmark
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 474)：
	./$(REDIS_BENCHMARK_NAME)

# 不是一个目标：
acl.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.sh:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

# 不是一个目标：
tracking.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
config.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
debug.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
commands.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.S.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

# 不是一个目标：
.mod:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

# 不是一个目标：
.mod.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

# 不是一个目标：
ae.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.F.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

# 不是一个目标：
cli_common.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

gcov:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 483)：
	$(MAKE) REDIS_CFLAGS="-fprofile-arcs -ftest-coverage -DCOVERAGE_TEST" REDIS_LDFLAGS="-fprofile-arcs -ftest-coverage"

# 不是一个目标：
t_stream.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.w:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.S.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

# 不是一个目标：
connection.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

check: test
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
zmalloc.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
socket.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
object.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

persist-settings: distclean
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 364)：
	echo STD=$(STD) >> .make-settings
	echo WARN=$(WARN) >> .make-settings
	echo OPT=$(OPT) >> .make-settings
	echo MALLOC=$(MALLOC) >> .make-settings
	echo BUILD_TLS=$(BUILD_TLS) >> .make-settings
	echo USE_SYSTEMD=$(USE_SYSTEMD) >> .make-settings
	echo CFLAGS=$(CFLAGS) >> .make-settings
	echo LDFLAGS=$(LDFLAGS) >> .make-settings
	echo REDIS_CFLAGS=$(REDIS_CFLAGS) >> .make-settings
	echo REDIS_LDFLAGS=$(REDIS_LDFLAGS) >> .make-settings
	echo PREV_FINAL_CFLAGS=$(FINAL_CFLAGS) >> .make-settings
	echo PREV_FINAL_LDFLAGS=$(FINAL_LDFLAGS) >> .make-settings
	-(cd ../deps && $(MAKE) $(DEPENDENCY_TARGETS))

# 不是一个目标：
multi.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
config.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
util.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.F:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
debug.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
adlist.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
tls.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.web:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
syncio.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
setproctitle.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
localtime.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

noopt:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 486)：
	$(MAKE) OPTIMIZATION="-O0"

# 不是一个目标：
.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

redis-tls.so: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 411)：
	$(QUIET_CC)$(CC) -o $@ tls.c -shared -fPIC $(TLS_MODULE_CFLAGS) $(TLS_CLIENT_LIBS)

# 不是一个目标：
anet.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

install: all
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 495)：
	@mkdir -p $(INSTALL_BIN)
	$(call MAKE_INSTALL,$(REDIS_SERVER_NAME),$(INSTALL_BIN))
	$(call MAKE_INSTALL,$(REDIS_BENCHMARK_NAME),$(INSTALL_BIN))
	$(call MAKE_INSTALL,$(REDIS_CLI_NAME),$(INSTALL_BIN))
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_CHECK_RDB_NAME)
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_CHECK_AOF_NAME)
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_SENTINEL_NAME)

# 不是一个目标：
strl.o:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 文件杂凑表状态:
# 负载=176/1024=17%, 重新杂凑=0, 碰撞=1900/7297=26%
# VPATH 搜索路径

# 没有“vpath”搜索路径。

# 没有通用搜索路径(“VPATH”变量)。

# strcache 缓冲: 9 (7) / 字串 = 4732 / 空间 = 72667 B / 平均 = 15 B
# 目前缓冲: 尺寸 = 8162 B / 已用 = 7456 B / 数量 = 395 / 平均 = 18 B
# 其他已用: 总共 = 65211 B / 数量 = 4337 / 平均 = 15 B
# 其他空余: 总共 = 85 B / 最大 = 16 B / 最小 = 2 B / 平均 = 10 B

# strcache 性能：查找 = 7689 / 命中率 = 38%
# 杂凑表统计数据:
# 负载=4732/8192=58%, 重新杂凑=0, 碰撞=4577/7689=60%
# 在 Wed Mar 15 10:42:41 2023
 上完成 make 数据基础

Hint: It's a good idea to run 'make test' ;)

# GNU Make 4.2.1
# 为 x86_64-pc-linux-gnu 编译
# Copyright (C) 1988-2016 Free Software Foundation, Inc.
# 许可证：GPLv3+：GNU 通用公共许可证第 3 版或更新版本<http://gnu.org/licenses/gpl.html>。
# 本软件是自由软件：您可以自由修改和重新发布它。
# 在法律允许的范围内没有其他保证。

# make 数据基础，打印在 Wed Mar 15 10:42:52 2023

# 变量

# 环境
VSCODE_GIT_ASKPASS_MAIN = /usr/share/code/resources/app/extensions/git/dist/askpass-main.js
# “override”指令
GNUMAKEFLAGS := 
# makefile (从“Makefile”，行 228)
LIBSYSTEMD_LIBS = -lsystemd
# 自动
<D = $(patsubst %/,%,$(dir $<))
# 自动
?F = $(notdir $?)
# 环境
DESKTOP_SESSION = ubuntu
# 默认
.SHELLFLAGS := -c
# 环境
XDG_SESSION_CLASS = user
# makefile (从“Makefile”，行 266)
FINAL_LIBS := ../deps/jemalloc/lib/libjemalloc.a -lm -ldl -pthread -lrt
# makefile (从“Makefile”，行 330)
QUIET_INSTALL = @printf '    %b %b\n' $(LINKCOLOR)INSTALL$(ENDCOLOR) $(BINCOLOR)$@$(ENDCOLOR) 1>&2;
# makefile (从“Makefile”，行 344)
REDIS_CHECK_RDB_NAME = redis-check-rdb$(PROG_SUFFIX)
# 环境
XDG_SESSION_TYPE = x11
# 默认
CWEAVE = cweave
# 环境
NVM_INC = /home/michael/.nvm/versions/node/v17.9.0/include/node
# 自动
?D = $(patsubst %/,%,$(dir $?))
# 环境
IM_CONFIG_PHASE = 1
# makefile (从“Makefile”，行 55)
INSTALL = install
# 自动
@D = $(patsubst %/,%,$(dir $@))
# 环境
XAUTHORITY = /run/user/1000/gdm/Xauthority
# 环境
GDMSESSION = ubuntu
# 环境
XMODIFIERS = @im=ibus
# makefile
CURDIR := /usr/local/gitcode/myLinuxFile/myRedis/src
# makefile
SHELL = /bin/sh
# 默认
RM = rm -f
# 默认
CO = co
# 环境
JOURNAL_STREAM = 8:51226
# makefile (从“Makefile”，行 18)
OPTIMIZATION = -O0
# 默认
PREPROCESS.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -F
# 默认
LINK.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
XDG_CONFIG_DIRS = /etc/xdg/xdg-ubuntu:/etc/xdg
# 默认
OUTPUT_OPTION = -o $@
# makefile (从“Makefile”，行 295)
TLS_MODULE_NAME := redis-tls.so
# makefile (从“Makefile”，行 287)
BUILD_MODULE := 2
# makefile (从“.make-settings”，行 11)
PREV_FINAL_CFLAGS = -pedantic -DREDIS_STATIC= -std=c11 -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations -O3 -g -ggdb -flto -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# 默认
COMPILE.cpp = $(COMPILE.cc)
# makefile (从“Makefile.dep”，行 1)
MAKEFILE_LIST :=  Makefile .make-settings Makefile.dep
# makefile (从“Makefile”，行 24)
NODEPS := clean distclean
# 自动
@F = $(notdir $@)
# makefile (从“Makefile”，行 305)
define MAKE_INSTALL
        @printf '    %b %b\n' $(LINKCOLOR)INSTALL$(ENDCOLOR) $(BINCOLOR)$(1)$(ENDCOLOR) 1>&2
        @$(INSTALL) $(1) $(2)
endef
# 环境
GIO_LAUNCHED_DESKTOP_FILE = /usr/share/applications/code.desktop
# makefile (从“Makefile”，行 117)
DEBUG = -g -ggdb
# 环境
USERNAME = michael
# 默认
LINK.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
MAKELEVEL := 1
# 环境
XDG_DATA_DIRS = /usr/share/ubuntu:/usr/local/share/:/usr/share/:/var/lib/snapd/desktop
# makefile (从“Makefile”，行 296)
TLS_MODULE_CFLAGS := -pedantic -DREDIS_STATIC= -std=c11 -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations -O3 -g -ggdb  -flto -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# 环境
DBUS_SESSION_BUS_ADDRESS = unix:path=/run/user/1000/bus
# 默认
CC = cc
# 环境
_ = /usr/bin/make
# makefile (从“Makefile”，行 56)
PKG_CONFIG = pkg-config
# 默认
CHECKOUT,v = +$(if $(wildcard $@),,$(CO) $(COFLAGS) $< $@)
# 环境
MANAGERPID = 1574
# makefile (从“Makefile”，行 233)
LIBSYSTEMD_PKGCONFIG := 
# 环境
LESSOPEN = | /usr/bin/lesspipe %s
# makefile (从“.make-settings”，行 6)
USE_SYSTEMD = 
# 环境
LC_NAME = zh_CN.UTF-8
# 默认
CPP = $(CC) -E
# 默认
LINK.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
MAKE_HOST := x86_64-pc-linux-gnu
# makefile (从“Makefile”，行 345)
REDIS_CHECK_AOF_NAME = redis-check-aof$(PROG_SUFFIX)
# 环境
PATH = /home/michael/.nvm/versions/node/v17.9.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
# 默认
LD = ld
# makefile (从“Makefile”，行 324)
ENDCOLOR = "\033[0m"
# 默认
TEXI2DVI = texi2dvi
# 环境
GPG_AGENT_INFO = /run/user/1000/gnupg/S.gpg-agent:0:1
# 默认
YACC = yacc
# 环境
SESSION_MANAGER = local/michael-CW65S:@/tmp/.ICE-unix/1776,unix/michael-CW65S:/tmp/.ICE-unix/1776
# makefile (从“Makefile”，行 336)
REDIS_SERVER_OBJ = server.o setproctitle.o zmalloc.o crcspeed.o crc64.o ./Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
# makefile (从“Makefile”，行 280)
LIBCRYPTO_LIBS = $(shell $(PKG_CONFIG) --libs libcrypto)
# 默认
COMPILE.mod = $(M2C) $(M2FLAGS) $(MODFLAGS) $(TARGET_ARCH)
# 环境
XDG_RUNTIME_DIR = /run/user/1000
# makefile (从“.make-settings”，行 7)
CFLAGS = 
# 默认
ARFLAGS = rv
# 默认
LINK.r = $(FC) $(FFLAGS) $(RFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
LINT = lint
# 默认
COMPILE.f = $(FC) $(FFLAGS) $(TARGET_ARCH) -c
# 默认
LINT.c = $(LINT) $(LINTFLAGS) $(CPPFLAGS) $(TARGET_ARCH)
# 默认
YACC.m = $(YACC) $(YFLAGS)
# 环境
NVM_BIN = /home/michael/.nvm/versions/node/v17.9.0/bin
# 默认
YACC.y = $(YACC) $(YFLAGS)
# makefile (从“Makefile”，行 273)
LIBSSL_LIBS = $(shell $(PKG_CONFIG) --libs libssl)
# makefile (从“Makefile”，行 264)
DEPENDENCY_TARGETS = hiredis linenoise lua hdr_histogram fpconv jemalloc
# 默认
AR = ar
# 环境
XDG_MENU_PREFIX = gnome-
# 环境
LC_NUMERIC = zh_CN.UTF-8
# 环境
INVOCATION_ID = ecb31a0f7d0042b2b3f1373d3de7451c
# makefile (从“.make-settings”，行 8)
LDFLAGS = 
# 默认
.FEATURES := target-specific order-only second-expansion else-if shortest-stem undefine oneshell archives jobserver output-sync check-symlink load
# 默认
TANGLE = tangle
# makefile (从“Makefile”，行 15)
release_hdr := #define REDIS_GIT_SHA1 "f69f6aac" #define REDIS_GIT_DIRTY "27"
# 环境
LS_COLORS = rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=30;41:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:
# 环境
XDG_SESSION_DESKTOP = ubuntu
# 环境
SSH_AUTH_SOCK = /run/user/1000/keyring/ssh
# 环境
GIO_LAUNCHED_DESKTOP_FILE_PID = 4959
# 默认
GET = get
# makefile (从“Makefile”，行 320)
LINKCOLOR = "\033[34;1m"
# 自动
%F = $(notdir $%)
# 环境
DISPLAY = :0
# 默认
COMPILE.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“Makefile”，行 227)
BUILD_WITH_SYSTEMD = no
# makefile (从“Makefile”，行 294)
TLS_MODULE = 
# 默认
CTANGLE = ctangle
# makefile (从“Makefile”，行 43)
NUMBER_SIGN_CHAR := #
# makefile (从“.make-settings”，行 3)
OPT = -O3
# 默认
.LIBPATTERNS = lib%.so lib%.a
# makefile (从“Makefile”，行 323)
MAKECOLOR = "\033[32;1m"
# 默认
LINK.C = $(LINK.cc)
# 环境
PWD = /usr/local/gitcode/myLinuxFile/myRedis/src
# makefile (从“Makefile”，行 319)
CCCOLOR = "\033[34m"
# 默认
.LOADED := 
# 默认
LINK.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 默认
PREPROCESS.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -F
# 环境
SSH_AGENT_PID = 1746
# 环境
ORIGINAL_XDG_CURRENT_DESKTOP = ubuntu:GNOME
# 环境
GTK_MODULES = gail:atk-bridge
# makefile (从“Makefile”，行 316)
REDIS_LD = $(QUIET_LINK)$(CC) $(FINAL_LDFLAGS)
# makefile (从“Makefile”，行 59)
PYTHON := /usr/bin/python3
# 默认
LINK.s = $(CC) $(ASFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 环境
HOME = /home/michael
# 环境
LESSCLOSE = /usr/bin/lesspipe %s %s
# makefile (从“Makefile”，行 278)
LIBCRYPTO_PKGCONFIG := 0
# makefile (从“Makefile”，行 203)
FINAL_LDFLAGS = $(LDFLAGS) $(REDIS_LDFLAGS) $(DEBUG) -rdynamic
# makefile (从“.make-settings”，行 2)
WARN = -Wall -W -Wno-missing-field-initializers -Werror=deprecated-declarations
# 环境
LOGNAME = michael
# 环境
NVM_CD_FLAGS = 
# 自动
+F = $(notdir $+)
# makefile (从“Makefile”，行 54)
INSTALL_BIN = $(PREFIX)/bin
# makefile (从“Makefile”，行 53)
PREFIX = /usr/local
# makefile (从“Makefile”，行 333)
REDIS_SERVER_NAME = redis-server$(PROG_SUFFIX)
# 环境
GNOME_DESKTOP_SESSION_ID = this-is-deprecated
# 自动
^D = $(patsubst %/,%,$(dir $^))
# makefile (从“Makefile”，行 271)
LIBSSL_PKGCONFIG := 0
# makefile (从“Makefile”，行 315)
REDIS_CC = $(QUIET_CC)$(CC) $(FINAL_CFLAGS)
# 环境
LC_TIME = zh_CN.UTF-8
# 环境
VSCODE_GIT_IPC_HANDLE = /run/user/1000/vscode-git-6c0f1317c6.sock
# 默认
COMPILE.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“.make-settings”，行 10)
REDIS_LDFLAGS = -flto
# 环境
COLORTERM = truecolor
# makefile (从“Makefile”，行 328)
QUIET_GEN = @printf '    %b %b\n' $(CCCOLOR)GEN$(ENDCOLOR) $(SRCCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
MAKE = $(MAKE_COMMAND)
# makefile (从“Makefile”，行 44)
C11_ATOMIC := yes
# 默认
MAKECMDGOALS := all
# 环境
QT_IM_MODULE = ibus
# 环境
SHLVL = 1
# makefile (从“Makefile”，行 338)
REDIS_CLI_NAME = redis-cli$(PROG_SUFFIX)
# 默认
AS = as
# 默认
PREPROCESS.S = $(CC) -E $(CPPFLAGS)
# 环境
GNOME_SHELL_SESSION_MODE = ubuntu
# 默认
COMPILE.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 默认
MAKE_VERSION := 4.2.1
# 环境
USER = michael
# makefile (从“Makefile”，行 327)
QUIET_CC = @printf '    %b %b\n' $(CCCOLOR)CC$(ENDCOLOR) $(SRCCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
FC = f77
# 环境
LC_MONETARY = zh_CN.UTF-8
# makefile
.DEFAULT_GOAL := all
# 环境
XDG_CURRENT_DESKTOP = Unity
# makefile (从“.make-settings”，行 12)
PREV_FINAL_LDFLAGS = -flto -g -ggdb -rdynamic
# makefile (从“Makefile”，行 340)
REDIS_CLI_OBJ = redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o anet.o ae.o siphash.o crc64.o strl.o cli_common.o monotonic.o crcspeed.o redisassert.o
# 自动
%D = $(patsubst %/,%,$(dir $%))
# 环境
LC_TELEPHONE = zh_CN.UTF-8
# makefile (从“Makefile”，行 286)
BUILD_YES := 1
# makefile (从“.make-settings”，行 9)
REDIS_CFLAGS = -flto
# 默认
WEAVE = weave
# 默认
MAKE_COMMAND := make
# 默认
LINK.cpp = $(LINK.cc)
# makefile (从“.make-settings”，行 1)
STD = -pedantic -DREDIS_STATIC= -std=c11
# 默认
F77 = $(FC)
# 环境
OLDPWD = /usr/local/gitcode/myLinuxFile/myRedis
# 环境
VSCODE_GIT_ASKPASS_EXTRA_ARGS = --ms-enable-electron-run-as-node
# 环境
TERM_PROGRAM = vscode
# 默认
.VARIABLES := 
# 默认
PC = pc
# 自动
*F = $(notdir $*)
# 环境
LANGUAGE = zh_CN:zh:en_US:en
# makefile (从“Makefile”，行 17)
uname_M := x86_64
# 环境
MAKE_RESTARTS = 1
# 环境
VSCODE_GIT_ASKPASS_NODE = /usr/share/code/code
# 默认
COMPILE.def = $(M2C) $(M2FLAGS) $(DEFFLAGS) $(TARGET_ARCH)
# 默认
LEX = lex
# makefile
MAKEFLAGS = pw
# 环境
MFLAGS = -pw
# 自动
*D = $(patsubst %/,%,$(dir $*))
# 环境
TERM_PROGRAM_VERSION = 1.76.0
# 环境
NVM_DIR = /home/michael/.nvm
# 默认
LEX.l = $(LEX) $(LFLAGS) -t
# 默认
LEX.m = $(LEX) $(LFLAGS) -t
# 自动
+D = $(patsubst %/,%,$(dir $+))
# 默认
COMPILE.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -c
# 环境
GDK_BACKEND = x11
# 环境
Dotnet_Dir = /home/michael/.dotnet/tools
# 默认
M2C = m2c
# 环境
LC_IDENTIFICATION = zh_CN.UTF-8
# 环境
QT_ACCESSIBILITY = 1
# makefile (从“Makefile”，行 343)
REDIS_BENCHMARK_OBJ = server.o
# “override”指令
.SHELLSTATUS := 0
# 环境
GIT_ASKPASS = /usr/share/code/resources/app/extensions/git/dist/askpass.sh
# 默认
MAKEFILES := 
# makefile (从“Makefile”，行 421)
DEP = $(REDIS_SERVER_OBJ:%.o=%.d) $(REDIS_CLI_OBJ:%.o=%.d) $(REDIS_BENCHMARK_OBJ:%.o=%.d)
# 默认
COMPILE.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 自动
<F = $(notdir $<)
# makefile (从“Makefile”，行 285)
BUILD_NO := 0
# makefile (从“Makefile”，行 334)
REDIS_SENTINEL_NAME = redis-sentinel$(PROG_SUFFIX)
# 自动
^F = $(notdir $^)
# makefile (从“.make-settings”，行 4)
MALLOC = jemalloc
# 默认
CXX = g++
# 环境
LC_MEASUREMENT = zh_CN.UTF-8
# makefile (从“Makefile”，行 341)
REDIS_BENCHMARK_NAME = redis-benchmark$(PROG_SUFFIX)
# 默认
COFLAGS = 
# makefile (从“Makefile”，行 317)
REDIS_INSTALL = $(QUIET_INSTALL)$(INSTALL)
# makefile (从“Makefile”，行 16)
uname_S := Linux
# 默认
COMPILE.C = $(COMPILE.cc)
# makefile (从“Makefile”，行 321)
SRCCOLOR = "\033[33m"
# 环境
GJS_DEBUG_TOPICS = JS ERROR;JS LOG
# 环境
LC_PAPER = zh_CN.UTF-8
# 默认
COMPILE.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(TARGET_MACH) -c
# 默认
LINK.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# makefile (从“Makefile”，行 346)
ALL_SOURCES = $(sort $(patsubst %.o,%.c,$(REDIS_SERVER_OBJ) $(REDIS_CLI_OBJ) $(REDIS_BENCHMARK_OBJ)))
# makefile (从“Makefile”，行 329)
QUIET_LINK = @printf '    %b %b\n' $(LINKCOLOR)LINK$(ENDCOLOR) $(BINCOLOR)$@$(ENDCOLOR) 1>&2;
# 默认
SUFFIXES := .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# 默认
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# makefile (从“.make-settings”，行 5)
BUILD_TLS = 
# 环境
CHROME_DESKTOP = code-url-handler.desktop
# 默认
LINK.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
WINDOWPATH = 2
# 默认
COMPILE.s = $(AS) $(ASFLAGS) $(TARGET_MACH)
# 默认
.INCLUDE_DIRS = /usr/include /usr/local/include /usr/include
# 环境
GJS_DEBUG_OUTPUT = stderr
# 默认
.RECIPEPREFIX := 
# 默认
MAKEINFO = makeinfo
# 环境
MAKE_TERMERR = /dev/pts/2
# 默认
OBJC = cc
# 环境
LC_ADDRESS = zh_CN.UTF-8
# 默认
LINK.f = $(FC) $(FFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
TEX = tex
# 环境
LANG = zh_CN.UTF-8
# 环境
TERM = xterm-256color
# 默认
F77FLAGS = $(FFLAGS)
# 默认
LINK.o = $(CC) $(LDFLAGS) $(TARGET_ARCH)
# makefile (从“Makefile”，行 265)
FINAL_CFLAGS = $(STD) $(WARN) $(OPT) $(DEBUG) $(CFLAGS) $(REDIS_CFLAGS) -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -I../deps/hdr_histogram -I../deps/fpconv -DUSE_JEMALLOC -I../deps/jemalloc/include
# makefile (从“Makefile”，行 322)
BINCOLOR = "\033[37;1m"
# 变量的杂凑表状态:
# 负载=240/1024=23%, 重新杂凑=0, 碰撞=480/1065=45%

# Pattern-specific 变量值

# 没有 pattern-specific 变量的值。

# 目录

# ../utils (设备 2053，i-节点 6171221)： 3 文件， 19 不可能.
# Main (设备 2053，i-节点 6031162)： 5 文件， 22 不可能.
# ../deps/fpconv/SCCS：无法对其进行 stat 操作。
# SCCS：无法对其进行 stat 操作。
# ../deps/linenoise (设备 2053，i-节点 6030973)： 9 文件， 无 不可能.
# ../utils/SCCS：无法对其进行 stat 操作。
# Main/SCCS：无法对其进行 stat 操作。
# . (设备 2053，i-节点 6030306)： 87 文件， 786 不可能.
# ../deps/fpconv/RCS：无法对其进行 stat 操作。
# ../deps/hiredis (设备 2053，i-节点 6030338)： 52 文件， 无 不可能.
# ../utils/RCS：无法对其进行 stat 操作。
# commands (设备 2053，i-节点 6171219)： 4 文件， 38 不可能.
# ../deps/linenoise/SCCS：无法对其进行 stat 操作。
# Main/RCS：无法对其进行 stat 操作。
# ../deps/hiredis/SCCS：无法对其进行 stat 操作。
# commands/SCCS：无法对其进行 stat 操作。
# ../deps/linenoise/RCS：无法对其进行 stat 操作。
# ../deps/hiredis/RCS：无法对其进行 stat 操作。
# commands/RCS：无法对其进行 stat 操作。
# ../deps/jemalloc/include/jemalloc (设备 2053，i-节点 6030452)： 19 文件， 无 不可能.
# ../deps/jemalloc/include/jemalloc/SCCS：无法对其进行 stat 操作。
# ../deps/jemalloc/include/jemalloc/RCS：无法对其进行 stat 操作。
# ../deps/fpconv (设备 2053，i-节点 6030317)： 10 文件， 无 不可能.
# RCS：无法对其进行 stat 操作。

# 189 文件， 865 不可能在 24 目录中。

# 隐含规则

%.o: %.c .make-prerequisites
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

%.out:

%.a:

%.ln:

%.o:

%: %.o
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.c:

%: %.c
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.ln: %.c
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

%.o: %.c
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

%.cc:

%: %.cc
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cc
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

%.C:

%: %.C
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.C
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

%.cpp:

%: %.cpp
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cpp
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

%.p:

%: %.p
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.p
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

%.f:

%: %.f
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.f
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

%.F:

%: %.F
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.F
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

%.f: %.F
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

%.m:

%: %.m
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.m
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

%.r:

%: %.r
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.r
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

%.f: %.r
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

%.y:

%.ln: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

%.c: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

%.l:

%.ln: %.l
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

%.c: %.l
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

%.r: %.l
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

%.ym:

%.m: %.ym
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

%.yl:

%.s:

%: %.s
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.s
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

%.S:

%: %.S
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.S
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

%.s: %.S
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

%.mod:

%: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

%.o: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

%.sym:

%.def:

%.sym: %.def
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

%.h:

%.info:

%.dvi:

%.tex:

%.dvi: %.tex
#  要执行的配方 (内置)：
	$(TEX) $<

%.texinfo:

%.info: %.texinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.texi:

%.info: %.texi
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texi
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.txinfo:

%.info: %.txinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.txinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.w:

%.c: %.w
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

%.tex: %.w
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

%.ch:

%.web:

%.p: %.web
#  要执行的配方 (内置)：
	$(TANGLE) $<

%.tex: %.web
#  要执行的配方 (内置)：
	$(WEAVE) $<

%.sh:

%: %.sh
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

%.elc:

%.el:

(%): %
#  要执行的配方 (内置)：
	$(AR) $(ARFLAGS) $@ $<

%.out: %
#  要执行的配方 (内置)：
	@rm -f $@ 
	 cp $< $@

%.c: %.w %.ch
#  要执行的配方 (内置)：
	$(CTANGLE) $^ $@

%.tex: %.w %.ch
#  要执行的配方 (内置)：
	$(CWEAVE) $^ $@

%:: %,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

%:: SCCS/s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

# 93 条隐含规则，5 (5.4%) 终端。

# 文件

# 不是一个目标：
monotonic.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:29:54.172519359
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
server.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
../deps/hiredis/read.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.688718
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
networking.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.web.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TANGLE) $<

# 不是一个目标：
sha256.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
syncio.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 15:08:21.782001452
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
rio.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:51:55.124011343
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
rax.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:50:15.188914333
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
anet.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 15:32:03.834953949
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.l.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

# 不是一个目标：
functions.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 19:50:00.880128583
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
sha256.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:55:34.210664182
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
unix.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
redisassert.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:08:06.03928636
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
version.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-29 11:37:34.168435514
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.ym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
../utils/generate-command-code.py:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-15 11:06:23.755138898
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.f.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

all: redis-server redis-cli
#  假目标 (.PHONY的前提)。
#  命令行目标。
#  对隐含规则的搜索尚未完成。
#  隐含/静态模式主干：“”
#  文件不存在。
#  文件已经被更新。
#  更新成功。
# 自动
# @ := all
# 自动
# % := 
# 自动
# * := 
# 自动
# + := redis-server redis-cli
# 自动
# | := 
# 自动
# < := redis-server
# 自动
# ^ := redis-server redis-cli
# 自动
# ? := redis-server redis-cli
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=1/626=0%
#  要执行的配方 (从“Makefile”，行 350)：
	@echo ""
	@echo "Hint: It's a good idea to run 'make test' ;)"
	@echo ""

# 不是一个目标：
.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
setproctitle.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
endianconv.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 15:47:39.910364473
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.y:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

sha256.o: sha256.c .make-prerequisites sha256.c sha256.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“sha256”
#  最近更新 2023-03-15 10:42:42.201976394
#  文件已经被更新。
#  更新成功。
# 自动
# @ := sha256.o
# 自动
# % := 
# 自动
# * := sha256
# 自动
# + := sha256.c .make-prerequisites sha256.c sha256.h
# 自动
# | := 
# 自动
# < := sha256.c
# 自动
# ^ := sha256.c .make-prerequisites sha256.h
# 自动
# ? := sha256.c .make-prerequisites sha256.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.def.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

anet.o: anet.c .make-prerequisites anet.c fmacros.h anet.h config.h util.h sds.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“anet”
#  最近更新 2023-03-15 10:42:44.271808085
#  文件已经被更新。
#  更新成功。
# 自动
# @ := anet.o
# 自动
# % := 
# 自动
# * := anet
# 自动
# + := anet.c .make-prerequisites anet.c fmacros.h anet.h config.h util.h sds.h
# 自动
# | := 
# 自动
# < := anet.c
# 自动
# ^ := anet.c .make-prerequisites fmacros.h anet.h config.h util.h sds.h
# 自动
# ? := anet.c .make-prerequisites fmacros.h anet.h config.h util.h sds.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
crc64.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
redis-cli.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
siphash.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

.PHONY: all persist-settings clean distclean lcov
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
../deps/linenoise/linenoise.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.708719
#  文件已经被更新。
#  更新成功。

ae.o: ae.c .make-prerequisites ae.c ae.h monotonic.h fmacros.h redisassert.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ae_epoll.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“ae”
#  最近更新 2023-03-15 10:42:42.821322288
#  文件已经被更新。
#  更新成功。
# 自动
# @ := ae.o
# 自动
# % := 
# 自动
# * := ae
# 自动
# + := ae.c .make-prerequisites ae.c ae.h monotonic.h fmacros.h redisassert.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ae_epoll.c
# 自动
# | := 
# 自动
# < := ae.c
# 自动
# ^ := ae.c .make-prerequisites ae.h monotonic.h fmacros.h redisassert.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ae_epoll.c
# 自动
# ? := ae.c .make-prerequisites ae.h monotonic.h fmacros.h redisassert.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ae_epoll.c
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

32bit:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 477)：
	@echo ""
	@echo "WARNING: if it fails under Linux you probably need to install libc6-dev-i386"
	@echo ""
	$(MAKE) CFLAGS="-m32" LDFLAGS="-m32"

# 不是一个目标：
.p.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

# 不是一个目标：
.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
monotonic.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:29:24.140056993
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.txinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

Main/mt19937-64.o: Main/mt19937-64.c .make-prerequisites
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“Main/mt19937-64”
#  最近更新 2023-01-15 12:44:34.024962293
#  文件已经被更新。
#  更新成功。
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

redis-sentinel: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 399)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_SENTINEL_NAME)

commands.c: commands/ping.json commands/client.json ../utils/generate-command-code.py
#  对隐含规则的搜索尚未完成。
#  最近更新 2023-03-14 22:26:40.414612953
#  文件已经被更新。
#  更新成功。
#  要执行的配方 (从“Makefile”，行 434)：
	$(QUIET_GEN)$(PYTHON) ../utils/generate-command-code.py

# 不是一个目标：
sds.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:17:13.608996198
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
dict.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.a:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

zmalloc.o: zmalloc.c .make-prerequisites zmalloc.c fmacros.h config.h solarisfixes.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h atomicvar.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“zmalloc”
#  最近更新 2023-03-15 10:42:41.754451943
#  文件已经被更新。
#  更新成功。
# 自动
# @ := zmalloc.o
# 自动
# % := 
# 自动
# * := zmalloc
# 自动
# + := zmalloc.c .make-prerequisites zmalloc.c fmacros.h config.h solarisfixes.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h atomicvar.h
# 自动
# | := 
# 自动
# < := zmalloc.c
# 自动
# ^ := zmalloc.c .make-prerequisites fmacros.h config.h solarisfixes.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h atomicvar.h
# 自动
# ? := zmalloc.c .make-prerequisites fmacros.h config.h solarisfixes.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h atomicvar.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

object.o: object.c .make-prerequisites object.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h functions.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“object”
#  最近更新 2023-03-15 10:42:43.065065585
#  文件已经被更新。
#  更新成功。
# 自动
# @ := object.o
# 自动
# % := 
# 自动
# * := object
# 自动
# + := object.c .make-prerequisites object.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h functions.h
# 自动
# | := 
# 自动
# < := object.c
# 自动
# ^ := object.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h functions.h
# 自动
# ? := object.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h functions.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.yl:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
cli_common.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 13:02:26.139628694
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.l.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

# 不是一个目标：
.F.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

multi.o: multi.c .make-prerequisites multi.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“multi”
#  最近更新 2023-03-15 10:42:44.479593394
#  文件已经被更新。
#  更新成功。
# 自动
# @ := multi.o
# 自动
# % := 
# 自动
# * := multi
# 自动
# + := multi.c .make-prerequisites multi.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := multi.c
# 自动
# ^ := multi.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := multi.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.texi.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.w.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

# 不是一个目标：
.texi.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
t_stream.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-13 13:26:13.901446566
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.ch:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

distclean: clean
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 444)：
	-(cd ../deps && $(MAKE) distclean)
	-(cd modules && $(MAKE) clean)
	-(cd ../tests/modules && $(MAKE) clean)
	-(rm -f .make-*)

# 不是一个目标：
redisassert.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:07:53.215252137
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
acl.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-15 11:30:18.199306677
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.m.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

redis-check-aof: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 407)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_CHECK_AOF_NAME)

monotonic.o: monotonic.c .make-prerequisites monotonic.c monotonic.h fmacros.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“monotonic”
#  最近更新 2023-03-15 10:42:42.869271648
#  文件已经被更新。
#  更新成功。
# 自动
# @ := monotonic.o
# 自动
# % := 
# 自动
# * := monotonic
# 自动
# + := monotonic.c .make-prerequisites monotonic.c monotonic.h fmacros.h
# 自动
# | := 
# 自动
# < := monotonic.c
# 自动
# ^ := monotonic.c .make-prerequisites monotonic.h fmacros.h
# 自动
# ? := monotonic.c .make-prerequisites monotonic.h fmacros.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.cc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

Makefile.dep:
#  对隐含规则的搜索尚未完成。
#  最近更新 2023-03-15 10:42:41.258981025
#  文件已经被更新。
#  更新成功。
#  要执行的配方 (从“Makefile”，行 355)：
	-$(REDIS_CC) -MM $(ALL_SOURCES) > Makefile.dep 2> /dev/null || true

# 不是一个目标：
atomicvar.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-11 18:33:53.193286238
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
../deps/hiredis/sdscompat.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.688718
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.cc.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

# 不是一个目标：
connhelpers.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 14:08:58.872847796
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
socket.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 15:53:19.46319628
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.def:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
endianconv.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 15:47:25.649965997
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.SUFFIXES: .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

localtime.o: localtime.c .make-prerequisites localtime.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“localtime”
#  最近更新 2023-03-15 10:42:41.85034954
#  文件已经被更新。
#  更新成功。
# 自动
# @ := localtime.o
# 自动
# % := 
# 自动
# * := localtime
# 自动
# + := localtime.c .make-prerequisites localtime.c
# 自动
# | := 
# 自动
# < := localtime.c
# 自动
# ^ := localtime.c .make-prerequisites
# 自动
# ? := localtime.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
strl.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

adlist.o: adlist.c .make-prerequisites adlist.c adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“adlist”
#  最近更新 2023-03-15 10:42:43.960130608
#  文件已经被更新。
#  更新成功。
# 自动
# @ := adlist.o
# 自动
# % := 
# 自动
# * := adlist
# 自动
# + := adlist.c .make-prerequisites adlist.c adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 自动
# | := 
# 自动
# < := adlist.c
# 自动
# ^ := adlist.c .make-prerequisites adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 自动
# ? := adlist.c .make-prerequisites adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
sdsalloc.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 13:14:25.79512203
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
../deps/fpconv/fpconv_dtoa.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.680718
#  文件已经被更新。
#  更新成功。

.make-prerequisites:
#  对隐含规则的搜索尚未完成。
#  最近更新 2023-01-11 21:44:37.697171879
#  文件已经被更新。
#  更新成功。
#  要执行的配方 (从“Makefile”，行 382)：
	@touch $@

# 不是一个目标：
.c.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

# 不是一个目标：
Makefile:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-15 10:14:33.54794888
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
util.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:53:52.014089058
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.r.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
unix.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 14:54:06.556201626
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
../deps/jemalloc/include/jemalloc/jemalloc.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-23 20:14:25.985181
#  文件已经被更新。
#  更新成功。

helgrind:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 492)：
	$(MAKE) OPTIMIZATION="-O0" MALLOC="libc" CFLAGS="-D__ATOMIC_VAR_FORCE_SYNC_MACROS" REDIS_CFLAGS="-I/usr/local/include" REDIS_LDFLAGS="-L/usr/local/lib"

server.o: server.c .make-prerequisites server.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“server”
#  最近更新 2023-03-15 10:42:41.618597014
#  文件已经被更新。
#  更新成功。
# 自动
# @ := server.o
# 自动
# % := 
# 自动
# * := server
# 自动
# + := server.c .make-prerequisites server.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := server.c
# 自动
# ^ := server.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := server.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

networking.o: networking.c .make-prerequisites networking.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h script.h ../deps/fpconv/fpconv_dtoa.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“networking”
#  最近更新 2023-03-15 10:42:43.796301742
#  文件已经被更新。
#  更新成功。
# 自动
# @ := networking.o
# 自动
# % := 
# 自动
# * := networking
# 自动
# + := networking.c .make-prerequisites networking.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h script.h ../deps/fpconv/fpconv_dtoa.h
# 自动
# | := 
# 自动
# < := networking.c
# 自动
# ^ := networking.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h script.h ../deps/fpconv/fpconv_dtoa.h
# 自动
# ? := networking.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h script.h ../deps/fpconv/fpconv_dtoa.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

debug.o: debug.c .make-prerequisites debug.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“debug”
#  最近更新 2023-03-15 10:42:42.565592371
#  文件已经被更新。
#  更新成功。
# 自动
# @ := debug.o
# 自动
# % := 
# 自动
# * := debug
# 自动
# + := debug.c .make-prerequisites debug.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := debug.c
# 自动
# ^ := debug.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := debug.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

test: redis-server redis-check-aof redis-cli redis-benchmark
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 452)：
	@(cd ..; ./runtest)

# 不是一个目标：
.ym.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
.y.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

# 不是一个目标：
cli_common.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 13:02:12.927594075
#  文件已经被更新。
#  更新成功。

test-modules: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 455)：
	@(cd ..; ./runtest-moduleapi)

unix.o: unix.c .make-prerequisites unix.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“unix”
#  最近更新 2023-03-15 10:42:43.396719143
#  文件已经被更新。
#  更新成功。
# 自动
# @ := unix.o
# 自动
# % := 
# 自动
# * := unix
# 自动
# + := unix.c .make-prerequisites unix.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := unix.c
# 自动
# ^ := unix.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := unix.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

strl.o: strl.c .make-prerequisites strl.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“strl”
#  最近更新 2023-03-15 10:42:44.291787443
#  文件已经被更新。
#  更新成功。
# 自动
# @ := strl.o
# 自动
# % := 
# 自动
# * := strl
# 自动
# + := strl.c .make-prerequisites strl.c
# 自动
# | := 
# 自动
# < := strl.c
# 自动
# ^ := strl.c .make-prerequisites
# 自动
# ? := strl.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
monotonic.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
connection.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:28:24.099086435
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.elc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

# 不是一个目标：
Main/mt19937-64.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
sds.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.out:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redis-cli: redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o anet.o ae.o siphash.o crc64.o strl.o cli_common.o monotonic.o crcspeed.o redisassert.o
#  对隐含规则的搜索尚未完成。
#  隐含/静态模式主干：“”
#  最近更新 2023-03-15 10:42:52.523629189
#  文件已经被更新。
#  更新成功。
# 自动
# @ := redis-cli
# 自动
# % := 
# 自动
# * := 
# 自动
# + := redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o anet.o ae.o siphash.o crc64.o strl.o cli_common.o monotonic.o crcspeed.o redisassert.o
# 自动
# | := 
# 自动
# < := redis-cli.o
# 自动
# ^ := redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o ae.o siphash.o crc64.o strl.o monotonic.o crcspeed.o redisassert.o
# 自动
# ? := redis-cli.o adlist.o dict.o anet.o zmalloc.o cli_common.o Main/mt19937-64.o ae.o siphash.o crc64.o strl.o monotonic.o crcspeed.o redisassert.o
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=6/78=8%
#  要执行的配方 (从“Makefile”，行 415)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/linenoise/linenoise.o $(FINAL_LIBS) $(TLS_CLIENT_LIBS)

crc64.o: crc64.c .make-prerequisites crc64.c crc64.h crcspeed.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“crc64”
#  最近更新 2023-03-15 10:42:41.82637514
#  文件已经被更新。
#  更新成功。
# 自动
# @ := crc64.o
# 自动
# % := 
# 自动
# * := crc64
# 自动
# + := crc64.c .make-prerequisites crc64.c crc64.h crcspeed.h
# 自动
# | := 
# 自动
# < := crc64.c
# 自动
# ^ := crc64.c .make-prerequisites crc64.h crcspeed.h
# 自动
# ? := crc64.c .make-prerequisites crc64.h crcspeed.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

test-sentinel: redis-sentinel redis-cli
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 458)：
	@(cd ..; ./runtest-sentinel)

redis-cli.o: redis-cli.c .make-prerequisites redis-cli.c fmacros.h version.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h ../deps/hiredis/sds.h dict.h Main/mt19937-64.h adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ../deps/linenoise/linenoise.h help.h anet.h ae.h monotonic.h connection.h cli_common.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“redis-cli”
#  最近更新 2023-03-15 10:42:49.85819222
#  文件已经被更新。
#  更新成功。
# 自动
# @ := redis-cli.o
# 自动
# % := 
# 自动
# * := redis-cli
# 自动
# + := redis-cli.c .make-prerequisites redis-cli.c fmacros.h version.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h ../deps/hiredis/sds.h dict.h Main/mt19937-64.h adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ../deps/linenoise/linenoise.h help.h anet.h ae.h monotonic.h connection.h cli_common.h
# 自动
# | := 
# 自动
# < := redis-cli.c
# 自动
# ^ := redis-cli.c .make-prerequisites fmacros.h version.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h dict.h Main/mt19937-64.h adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ../deps/linenoise/linenoise.h help.h anet.h ae.h monotonic.h connection.h cli_common.h
# 自动
# ? := redis-cli.c .make-prerequisites fmacros.h version.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h dict.h Main/mt19937-64.h adlist.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h ../deps/linenoise/linenoise.h help.h anet.h ae.h monotonic.h connection.h cli_common.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

siphash.o: siphash.c .make-prerequisites siphash.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“siphash”
#  最近更新 2023-03-15 10:42:42.62153329
#  文件已经被更新。
#  更新成功。
# 自动
# @ := siphash.o
# 自动
# % := 
# 自动
# * := siphash
# 自动
# + := siphash.c .make-prerequisites siphash.c
# 自动
# | := 
# 自动
# < := siphash.c
# 自动
# ^ := siphash.c .make-prerequisites
# 自动
# ? := siphash.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.C:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
redisassert.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
util.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:29:12.355829807
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.r.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
multi.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.S:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

lcov:
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 466)：
	$(MAKE) gcov
	@(set -e; cd ..; ./runtest --clients 1)
	@geninfo -o redis.info .
	@genhtml --legend -o lcov-html redis.info

# 不是一个目标：
commands/ping.json:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-15 11:04:21.186786444
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
acl.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

valgrind:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 489)：
	$(MAKE) OPTIMIZATION="-O0" MALLOC="libc"

# 不是一个目标：
.texinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.w.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

# 不是一个目标：
.c.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

# 不是一个目标：
.s.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

# 不是一个目标：
.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
endianconv.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

dict.o: dict.c .make-prerequisites dict.c dict.h Main/mt19937-64.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h redisassert.h config.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“dict”
#  最近更新 2023-03-15 10:42:42.445718972
#  文件已经被更新。
#  更新成功。
# 自动
# @ := dict.o
# 自动
# % := 
# 自动
# * := dict
# 自动
# + := dict.c .make-prerequisites dict.c dict.h Main/mt19937-64.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h redisassert.h config.h
# 自动
# | := 
# 自动
# < := dict.c
# 自动
# ^ := dict.c .make-prerequisites dict.h Main/mt19937-64.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h redisassert.h config.h
# 自动
# ? := dict.c .make-prerequisites dict.h Main/mt19937-64.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h redisassert.h config.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
sds.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 13:05:49.965353943
#  文件已经被更新。
#  更新成功。

setproctitle.o: setproctitle.c .make-prerequisites setproctitle.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“setproctitle”
#  最近更新 2023-03-15 10:42:41.670541545
#  文件已经被更新。
#  更新成功。
# 自动
# @ := setproctitle.o
# 自动
# % := 
# 自动
# * := setproctitle
# 自动
# + := setproctitle.c .make-prerequisites setproctitle.c
# 自动
# | := 
# 自动
# < := setproctitle.c
# 自动
# ^ := setproctitle.c .make-prerequisites
# 自动
# ? := setproctitle.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
ae.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 14:50:06.11345516
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
tracking.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
../deps/hiredis/sds.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.688718
#  文件已经被更新。
#  更新成功。

redis-benchmark: server.o
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 419)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/hdr_histogram/libhdrhistogram.a $(FINAL_LIBS) $(TLS_CLIENT_LIBS)

crcspeed.o: crcspeed.c .make-prerequisites crcspeed.c crcspeed.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“crcspeed”
#  最近更新 2023-03-15 10:42:41.798405008
#  文件已经被更新。
#  更新成功。
# 自动
# @ := crcspeed.o
# 自动
# % := 
# 自动
# * := crcspeed
# 自动
# + := crcspeed.c .make-prerequisites crcspeed.c crcspeed.h
# 自动
# | := 
# 自动
# < := crcspeed.c
# 自动
# ^ := crcspeed.c .make-prerequisites crcspeed.h
# 自动
# ? := crcspeed.c .make-prerequisites crcspeed.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
Main/mt19937-64.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:39:48.965041899
#  文件已经被更新。
#  更新成功。

mt19937-64.o: Main/mt19937-64.c Main/mt19937-64.h Main/mt19937-64.c Main/mt19937-64.h
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
commands.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
commands/client.json:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 22:26:37.002613934
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
../deps/hiredis/alloc.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.684718
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
zmalloc.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 11:43:12.870510304
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
object.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-14 14:34:23.511816833
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.texinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.el:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redis-check-rdb: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 403)：
	$(REDIS_INSTALL) $(REDIS_SERVER_NAME) $(REDIS_CHECK_RDB_NAME)

# 不是一个目标：
multi.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 21:06:56.047317035
#  文件已经被更新。
#  更新成功。

uninstall:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 504)：
	rm -f $(INSTALL_BIN)/{$(REDIS_SERVER_NAME),$(REDIS_BENCHMARK_NAME),$(REDIS_CLI_NAME),$(REDIS_CHECK_RDB_NAME),$(REDIS_CHECK_AOF_NAME),$(REDIS_SENTINEL_NAME)}

# 不是一个目标：
ae_epoll.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-30 12:04:42.355808921
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.lm.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.m) $< > $@

# 不是一个目标：
cli_common.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.make-settings:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-11 21:44:31.213406742
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.y.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
t_stream.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.web.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(WEAVE) $<

# 不是一个目标：
.texinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
adlist.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:22:36.175824775
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
connection.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.DEFAULT:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
tls.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 14:58:32.392782644
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.h:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
socket.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
setproctitle.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 15:24:20.267573715
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
localtime.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 13:21:34.096793037
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
stream.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-13 13:41:44.216037693
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
anet.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:30:40.301202083
#  文件已经被更新。
#  更新成功。

redis-server: server.o setproctitle.o zmalloc.o crcspeed.o crc64.o Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
#  对隐含规则的搜索尚未完成。
#  隐含/静态模式主干：“”
#  最近更新 2023-03-15 10:42:49.614430887
#  文件已经被更新。
#  更新成功。
# 自动
# @ := redis-server
# 自动
# % := 
# 自动
# * := 
# 自动
# + := server.o setproctitle.o zmalloc.o crcspeed.o crc64.o Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
# 自动
# | := 
# 自动
# < := server.o
# 自动
# ^ := server.o setproctitle.o zmalloc.o crcspeed.o crc64.o Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
# 自动
# ? := server.o setproctitle.o zmalloc.o crcspeed.o crc64.o Main/mt19937-64.o localtime.o util.o sds.o sha256.o config.o dict.o debug.o siphash.o acl.o ae.o monotonic.o object.o connection.o socket.o unix.o syncio.o networking.o tls.o adlist.o tracking.o endianconv.o t_stream.o anet.o strl.o commands.o multi.o
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/553=1%
#  要执行的配方 (从“Makefile”，行 395)：
	$(REDIS_LD) -o $@ $^ ../deps/hiredis/libhiredis.a ../deps/lua/src/liblua.a ../deps/hdr_histogram/libhdrhistogram.a ../deps/fpconv/libfpconv.a $(FINAL_LIBS)

# 不是一个目标：
ae.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:29:03.119724443
#  文件已经被更新。
#  更新成功。

test-cluster: redis-server redis-cli
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 461)：
	@(cd ..; ./runtest-cluster)

bench: redis-benchmark
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 474)：
	./$(REDIS_BENCHMARK_NAME)

# 不是一个目标：
util.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
server.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 21:25:52.953147082
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
networking.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 17:42:11.627792743
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
Main/mt19937-64.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:44:31.548961382
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
tls.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
debug.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 17:13:11.521841542
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.tex.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEX) $<

# 不是一个目标：
.cpp.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

# 不是一个目标：
.cpp:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
syncio.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
../deps/hiredis/hiredis.h:
#  对隐含规则的搜索已完成。
#  最近更新 2022-11-22 10:59:31.688718
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
crcspeed.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

clean:
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 438)：
	rm -rf $(REDIS_SERVER_NAME) $(REDIS_SENTINEL_NAME) $(REDIS_CLI_NAME) $(REDIS_BENCHMARK_NAME) $(REDIS_CHECK_RDB_NAME) $(REDIS_CHECK_AOF_NAME) *.o *.gcda *.gcno *.gcov redis.info lcov-html Makefile.dep *.so
	rm -f $(DEP)

# 不是一个目标：
zmalloc.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 13:26:49.469186409
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.C.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

sds.o: sds.c .make-prerequisites sds.c sds.h sdsalloc.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“sds”
#  最近更新 2023-03-15 10:42:42.162018594
#  文件已经被更新。
#  更新成功。
# 自动
# @ := sds.o
# 自动
# % := 
# 自动
# * := sds
# 自动
# + := sds.c .make-prerequisites sds.c sds.h sdsalloc.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 自动
# | := 
# 自动
# < := sds.c
# 自动
# ^ := sds.c .make-prerequisites sds.h sdsalloc.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 自动
# ? := sds.c .make-prerequisites sds.h sdsalloc.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
redismodule.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-10 16:47:31.962917994
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.texi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
config.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 15:41:59.507756118
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.txinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
strl.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-13 13:44:46.823081302
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

redisassert.o: redisassert.c .make-prerequisites redisassert.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“redisassert”
#  最近更新 2023-03-15 10:42:49.958094406
#  文件已经被更新。
#  更新成功。
# 自动
# @ := redisassert.o
# 自动
# % := 
# 自动
# * := redisassert
# 自动
# + := redisassert.c .make-prerequisites redisassert.c
# 自动
# | := 
# 自动
# < := redisassert.c
# 自动
# ^ := redisassert.c .make-prerequisites
# 自动
# ? := redisassert.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.txinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
adlist.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:22:18.883368003
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
script.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 18:16:49.559320476
#  文件已经被更新。
#  更新成功。

install: all
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 495)：
	@mkdir -p $(INSTALL_BIN)
	$(call MAKE_INSTALL,$(REDIS_SERVER_NAME),$(INSTALL_BIN))
	$(call MAKE_INSTALL,$(REDIS_BENCHMARK_NAME),$(INSTALL_BIN))
	$(call MAKE_INSTALL,$(REDIS_CLI_NAME),$(INSTALL_BIN))
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_CHECK_RDB_NAME)
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_CHECK_AOF_NAME)
	@ln -sf $(REDIS_SERVER_NAME) $(INSTALL_BIN)/$(REDIS_SENTINEL_NAME)

# 不是一个目标：
tracking.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 21:18:24.37447339
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
crc64.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 12:14:20.58823802
#  文件已经被更新。
#  更新成功。

endianconv.o: endianconv.c .make-prerequisites endianconv.c
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“endianconv”
#  最近更新 2023-03-15 10:42:44.06801865
#  文件已经被更新。
#  更新成功。
# 自动
# @ := endianconv.o
# 自动
# % := 
# 自动
# * := endianconv
# 自动
# + := endianconv.c .make-prerequisites endianconv.c
# 自动
# | := 
# 自动
# < := endianconv.c
# 自动
# ^ := endianconv.c .make-prerequisites
# 自动
# ? := endianconv.c .make-prerequisites
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
redis-cli.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-14 11:23:44.970394014
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
siphash.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-28 12:44:38.509732512
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
rdb.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 18:40:17.583716115
#  文件已经被更新。
#  更新成功。

acl.o: acl.c .make-prerequisites acl.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h sha256.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“acl”
#  最近更新 2023-03-15 10:42:42.73741091
#  文件已经被更新。
#  更新成功。
# 自动
# @ := acl.o
# 自动
# % := 
# 自动
# * := acl
# 自动
# + := acl.c .make-prerequisites acl.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h sha256.h
# 自动
# | := 
# 自动
# < := acl.c
# 自动
# ^ := acl.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h sha256.h
# 自动
# ? := acl.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h sha256.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.sh:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

tracking.o: tracking.c .make-prerequisites tracking.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“tracking”
#  最近更新 2023-03-15 10:42:44.048039293
#  文件已经被更新。
#  更新成功。
# 自动
# @ := tracking.o
# 自动
# % := 
# 自动
# * := tracking
# 自动
# + := tracking.c .make-prerequisites tracking.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := tracking.c
# 自动
# ^ := tracking.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := tracking.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
server.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 21:19:44.813122367
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
sha256.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 12:55:19.4905846
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
connection.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 16:28:34.903265867
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
config.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

commands.o: commands.c .make-prerequisites commands.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“commands”
#  最近更新 2023-03-15 10:42:44.387688353
#  文件已经被更新。
#  更新成功。
# 自动
# @ := commands.o
# 自动
# % := 
# 自动
# * := commands
# 自动
# + := commands.c .make-prerequisites commands.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := commands.c
# 自动
# ^ := commands.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := commands.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
dict.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 23:15:46.434161791
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
fmacros.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 11:44:02.278636791
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.S.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

# 不是一个目标：
.mod:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

# 不是一个目标：
.mod.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

# 不是一个目标：
ae.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.F.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

cli_common.o: cli_common.c .make-prerequisites cli_common.c fmacros.h cli_common.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h ../deps/hiredis/sds.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“cli_common”
#  最近更新 2023-03-15 10:42:49.926125707
#  文件已经被更新。
#  更新成功。
# 自动
# @ := cli_common.o
# 自动
# % := 
# 自动
# * := cli_common
# 自动
# + := cli_common.c .make-prerequisites cli_common.c fmacros.h cli_common.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h ../deps/hiredis/sds.h
# 自动
# | := 
# 自动
# < := cli_common.c
# 自动
# ^ := cli_common.c .make-prerequisites fmacros.h cli_common.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h
# 自动
# ? := cli_common.c .make-prerequisites fmacros.h cli_common.h ../deps/hiredis/hiredis.h ../deps/hiredis/read.h ../deps/hiredis/sds.h ../deps/hiredis/alloc.h ../deps/hiredis/sdscompat.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
crcspeed.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 12:35:39.834201452
#  文件已经被更新。
#  更新成功。

gcov:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 483)：
	$(MAKE) REDIS_CFLAGS="-fprofile-arcs -ftest-coverage -DCOVERAGE_TEST" REDIS_LDFLAGS="-fprofile-arcs -ftest-coverage"

t_stream.o: t_stream.c .make-prerequisites t_stream.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h stream.h rax.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“t_stream”
#  最近更新 2023-03-15 10:42:44.171911303
#  文件已经被更新。
#  更新成功。
# 自动
# @ := t_stream.o
# 自动
# % := 
# 自动
# * := t_stream
# 自动
# + := t_stream.c .make-prerequisites t_stream.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h stream.h rax.h
# 自动
# | := 
# 自动
# < := t_stream.c
# 自动
# ^ := t_stream.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h stream.h rax.h
# 自动
# ? := t_stream.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h stream.h rax.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
config.c:
#  对隐含规则的搜索已完成。
#  最近更新 2023-03-14 18:50:44.282146602
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.w:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.S.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

connection.o: connection.c .make-prerequisites connection.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“connection”
#  最近更新 2023-03-15 10:42:43.160965409
#  文件已经被更新。
#  更新成功。
# 自动
# @ := connection.o
# 自动
# % := 
# 自动
# * := connection
# 自动
# + := connection.c .make-prerequisites connection.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := connection.c
# 自动
# ^ := connection.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := connection.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

check: test
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
zmalloc.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

socket.o: socket.c .make-prerequisites socket.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“socket”
#  最近更新 2023-03-15 10:42:43.292827667
#  文件已经被更新。
#  更新成功。
# 自动
# @ := socket.o
# 自动
# % := 
# 自动
# * := socket
# 自动
# + := socket.c .make-prerequisites socket.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 自动
# | := 
# 自动
# < := socket.c
# 自动
# ^ := socket.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 自动
# ? := socket.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
object.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

persist-settings: distclean
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 364)：
	echo STD=$(STD) >> .make-settings
	echo WARN=$(WARN) >> .make-settings
	echo OPT=$(OPT) >> .make-settings
	echo MALLOC=$(MALLOC) >> .make-settings
	echo BUILD_TLS=$(BUILD_TLS) >> .make-settings
	echo USE_SYSTEMD=$(USE_SYSTEMD) >> .make-settings
	echo CFLAGS=$(CFLAGS) >> .make-settings
	echo LDFLAGS=$(LDFLAGS) >> .make-settings
	echo REDIS_CFLAGS=$(REDIS_CFLAGS) >> .make-settings
	echo REDIS_LDFLAGS=$(REDIS_LDFLAGS) >> .make-settings
	echo PREV_FINAL_CFLAGS=$(FINAL_CFLAGS) >> .make-settings
	echo PREV_FINAL_LDFLAGS=$(FINAL_LDFLAGS) >> .make-settings
	-(cd ../deps && $(MAKE) $(DEPENDENCY_TARGETS))

# 不是一个目标：
crc64.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 12:14:15.056241607
#  文件已经被更新。
#  更新成功。

config.o: config.c .make-prerequisites config.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“config”
#  最近更新 2023-03-15 10:42:42.361807593
#  文件已经被更新。
#  更新成功。
# 自动
# @ := config.o
# 自动
# % := 
# 自动
# * := config
# 自动
# + := config.c .make-prerequisites config.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := config.c
# 自动
# ^ := config.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := config.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.l:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

util.o: util.c .make-prerequisites util.c fmacros.h ../deps/fpconv/fpconv_dtoa.h util.h sds.h sha256.h config.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“util”
#  最近更新 2023-03-15 10:42:42.006183175
#  文件已经被更新。
#  更新成功。
# 自动
# @ := util.o
# 自动
# % := 
# 自动
# * := util
# 自动
# + := util.c .make-prerequisites util.c fmacros.h ../deps/fpconv/fpconv_dtoa.h util.h sds.h sha256.h config.h
# 自动
# | := 
# 自动
# < := util.c
# 自动
# ^ := util.c .make-prerequisites fmacros.h ../deps/fpconv/fpconv_dtoa.h util.h sds.h sha256.h config.h
# 自动
# ? := util.c .make-prerequisites fmacros.h ../deps/fpconv/fpconv_dtoa.h util.h sds.h sha256.h config.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
.F:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
debug.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
adlist.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

tls.o: tls.c .make-prerequisites tls.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“tls”
#  最近更新 2023-03-15 10:42:43.912180696
#  文件已经被更新。
#  更新成功。
# 自动
# @ := tls.o
# 自动
# % := 
# 自动
# * := tls
# 自动
# + := tls.c .make-prerequisites tls.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 自动
# | := 
# 自动
# < := tls.c
# 自动
# ^ := tls.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 自动
# ? := tls.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h connhelpers.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
help.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-02-13 15:31:20.778022301
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
.web:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

syncio.o: syncio.c .make-prerequisites syncio.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“syncio”
#  最近更新 2023-03-15 10:42:43.48862314
#  文件已经被更新。
#  更新成功。
# 自动
# @ := syncio.o
# 自动
# % := 
# 自动
# * := syncio
# 自动
# + := syncio.c .make-prerequisites syncio.c server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# | := 
# 自动
# < := syncio.c
# 自动
# ^ := syncio.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 自动
# ? := syncio.c .make-prerequisites server.h fmacros.h config.h zmalloc.h ../deps/jemalloc/include/jemalloc/jemalloc.h endianconv.h crc64.h util.h sds.h dict.h Main/mt19937-64.h atomicvar.h adlist.h ae.h monotonic.h anet.h connection.h version.h redismodule.h rdb.h rio.h
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=5/28=18%
#  要执行的配方 (从“Makefile”，行 428)：
	$(REDIS_CC) -MMD -o $@ -c $<

# 不是一个目标：
dict.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-15 19:14:25.636599046
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
localtime.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

noopt:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 486)：
	$(MAKE) OPTIMIZATION="-O0"

# 不是一个目标：
.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

redis-tls.so: redis-server
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 411)：
	$(QUIET_CC)$(CC) -o $@ tls.c -shared -fPIC $(TLS_MODULE_CFLAGS) $(TLS_CLIENT_LIBS)

# 不是一个目标：
anet.d:
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新失败。

# 不是一个目标：
.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
solarisfixes.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-11 21:45:59.235059327
#  文件已经被更新。
#  更新成功。

# 不是一个目标：
crcspeed.h:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-14 12:14:58.936212436
#  文件已经被更新。
#  更新成功。

# 文件杂凑表状态:
# 负载=252/1024=25%, 重新杂凑=0, 碰撞=3431/9561=36%
# VPATH 搜索路径

# 没有“vpath”搜索路径。

# 没有通用搜索路径(“VPATH”变量)。

# strcache 缓冲: 13 (12) / 字串 = 6285 / 空间 = 100881 B / 平均 = 16 B
# 目前缓冲: 尺寸 = 8162 B / 已用 = 3041 B / 数量 = 180 / 平均 = 16 B
# 其他已用: 总共 = 97840 B / 数量 = 6105 / 平均 = 16 B
# 其他空余: 总共 = 104 B / 最大 = 14 B / 最小 = 3 B / 平均 = 8 B

# strcache 性能：查找 = 10343 / 命中率 = 39%
# 杂凑表统计数据:
# 负载=6285/8192=77%, 重新杂凑=0, 碰撞=9217/10343=89%
# 在 Wed Mar 15 10:42:52 2023
 上完成 make 数据基础
# make[1]: 离开目录“/usr/local/gitcode/myLinuxFile/myRedis/src”
# GNU Make 4.2.1
# 为 x86_64-pc-linux-gnu 编译
# Copyright (C) 1988-2016 Free Software Foundation, Inc.
# 许可证：GPLv3+：GNU 通用公共许可证第 3 版或更新版本<http://gnu.org/licenses/gpl.html>。
# 本软件是自由软件：您可以自由修改和重新发布它。
# 在法律允许的范围内没有其他保证。

# make 数据基础，打印在 Wed Mar 15 10:42:52 2023

# 变量

# 环境
VSCODE_GIT_ASKPASS_MAIN = /usr/share/code/resources/app/extensions/git/dist/askpass-main.js
# “override”指令
GNUMAKEFLAGS := 
# 自动
<D = $(patsubst %/,%,$(dir $<))
# 自动
?F = $(notdir $?)
# 环境
DESKTOP_SESSION = ubuntu
# 默认
.SHELLFLAGS := -c
# 环境
XDG_SESSION_CLASS = user
# 环境
XDG_SESSION_TYPE = x11
# 默认
CWEAVE = cweave
# 环境
NVM_INC = /home/michael/.nvm/versions/node/v17.9.0/include/node
# 自动
?D = $(patsubst %/,%,$(dir $?))
# 自动
@D = $(patsubst %/,%,$(dir $@))
# 环境
XAUTHORITY = /run/user/1000/gdm/Xauthority
# 环境
GDMSESSION = ubuntu
# 环境
XMODIFIERS = @im=ibus
# makefile
CURDIR := /usr/local/gitcode/myLinuxFile/myRedis
# makefile
SHELL = /bin/sh
# 默认
RM = rm -f
# 默认
CO = co
# 环境
JOURNAL_STREAM = 8:51226
# 默认
PREPROCESS.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -F
# 默认
LINK.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
XDG_CONFIG_DIRS = /etc/xdg/xdg-ubuntu:/etc/xdg
# 默认
OUTPUT_OPTION = -o $@
# 默认
COMPILE.cpp = $(COMPILE.cc)
# makefile (从“Makefile”，行 1)
MAKEFILE_LIST :=  Makefile
# 自动
@F = $(notdir $@)
# 环境
IM_CONFIG_PHASE = 1
# 环境
USERNAME = michael
# 默认
LINK.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
MAKELEVEL := 0
# 环境
XDG_DATA_DIRS = /usr/share/ubuntu:/usr/local/share/:/usr/share/:/var/lib/snapd/desktop
# 环境
DBUS_SESSION_BUS_ADDRESS = unix:path=/run/user/1000/bus
# 默认
CC = cc
# 环境
_ = /usr/bin/make
# 默认
CHECKOUT,v = +$(if $(wildcard $@),,$(CO) $(COFLAGS) $< $@)
# 环境
MANAGERPID = 1574
# 环境
LESSOPEN = | /usr/bin/lesspipe %s
# 环境
LC_NAME = zh_CN.UTF-8
# 默认
CPP = $(CC) -E
# 默认
LINK.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
MAKE_HOST := x86_64-pc-linux-gnu
# 环境
PATH = /home/michael/.nvm/versions/node/v17.9.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
# 默认
LD = ld
# 默认
TEXI2DVI = texi2dvi
# 环境
GPG_AGENT_INFO = /run/user/1000/gnupg/S.gpg-agent:0:1
# 默认
YACC = yacc
# 环境
SESSION_MANAGER = local/michael-CW65S:@/tmp/.ICE-unix/1776,unix/michael-CW65S:/tmp/.ICE-unix/1776
# 默认
COMPILE.mod = $(M2C) $(M2FLAGS) $(MODFLAGS) $(TARGET_ARCH)
# 环境
XDG_RUNTIME_DIR = /run/user/1000
# 默认
ARFLAGS = rv
# 默认
LINK.r = $(FC) $(FFLAGS) $(RFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
LINT = lint
# 默认
COMPILE.f = $(FC) $(FFLAGS) $(TARGET_ARCH) -c
# 默认
LINT.c = $(LINT) $(LINTFLAGS) $(CPPFLAGS) $(TARGET_ARCH)
# 默认
YACC.m = $(YACC) $(YFLAGS)
# 环境
NVM_BIN = /home/michael/.nvm/versions/node/v17.9.0/bin
# 默认
YACC.y = $(YACC) $(YFLAGS)
# 默认
AR = ar
# 环境
XDG_MENU_PREFIX = gnome-
# 环境
LC_NUMERIC = zh_CN.UTF-8
# 环境
INVOCATION_ID = ecb31a0f7d0042b2b3f1373d3de7451c
# 默认
.FEATURES := target-specific order-only second-expansion else-if shortest-stem undefine oneshell archives jobserver output-sync check-symlink load
# 默认
TANGLE = tangle
# 环境
LS_COLORS = rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=30;41:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:
# 环境
XDG_SESSION_DESKTOP = ubuntu
# 环境
SSH_AUTH_SOCK = /run/user/1000/keyring/ssh
# 环境
GIO_LAUNCHED_DESKTOP_FILE_PID = 4959
# 默认
GET = get
# 自动
%F = $(notdir $%)
# 环境
DISPLAY = :0
# 默认
COMPILE.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 默认
CTANGLE = ctangle
# 默认
.LIBPATTERNS = lib%.so lib%.a
# 默认
LINK.C = $(LINK.cc)
# 环境
PWD = /usr/local/gitcode/myLinuxFile/myRedis
# 默认
.LOADED := 
# 默认
LINK.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 默认
PREPROCESS.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -F
# 环境
SSH_AGENT_PID = 1746
# 环境
ORIGINAL_XDG_CURRENT_DESKTOP = ubuntu:GNOME
# 环境
GTK_MODULES = gail:atk-bridge
# 默认
LINK.s = $(CC) $(ASFLAGS) $(LDFLAGS) $(TARGET_MACH)
# 环境
HOME = /home/michael
# 环境
LESSCLOSE = /usr/bin/lesspipe %s %s
# 环境
LOGNAME = michael
# 环境
NVM_CD_FLAGS = 
# 自动
+F = $(notdir $+)
# 环境
GNOME_DESKTOP_SESSION_ID = this-is-deprecated
# 自动
^D = $(patsubst %/,%,$(dir $^))
# 环境
LC_TIME = zh_CN.UTF-8
# 环境
VSCODE_GIT_IPC_HANDLE = /run/user/1000/vscode-git-6c0f1317c6.sock
# 默认
COMPILE.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 环境
COLORTERM = truecolor
# 默认
MAKE = $(MAKE_COMMAND)
# 环境
QT_IM_MODULE = ibus
# 环境
SHLVL = 1
# 默认
AS = as
# 默认
PREPROCESS.S = $(CC) -E $(CPPFLAGS)
# 环境
GNOME_SHELL_SESSION_MODE = ubuntu
# 默认
COMPILE.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 默认
MAKE_VERSION := 4.2.1
# 环境
USER = michael
# 默认
FC = f77
# 环境
LC_MONETARY = zh_CN.UTF-8
# makefile
.DEFAULT_GOAL := default
# 环境
XDG_CURRENT_DESKTOP = Unity
# 自动
%D = $(patsubst %/,%,$(dir $%))
# 环境
LC_TELEPHONE = zh_CN.UTF-8
# 默认
WEAVE = weave
# 默认
MAKE_COMMAND := make
# 默认
LINK.cpp = $(LINK.cc)
# 默认
F77 = $(FC)
# 默认
COMPILE.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 环境
VSCODE_GIT_ASKPASS_EXTRA_ARGS = --ms-enable-electron-run-as-node
# 环境
TERM_PROGRAM = vscode
# 默认
.VARIABLES := 
# 默认
PC = pc
# 自动
*F = $(notdir $*)
# 环境
LANGUAGE = zh_CN:zh:en_US:en
# 环境
VSCODE_GIT_ASKPASS_NODE = /usr/share/code/code
# 默认
COMPILE.def = $(M2C) $(M2FLAGS) $(DEFFLAGS) $(TARGET_ARCH)
# 默认
LEX = lex
# makefile
MAKEFLAGS = p
# 环境
MFLAGS = -p
# 自动
*D = $(patsubst %/,%,$(dir $*))
# 环境
TERM_PROGRAM_VERSION = 1.76.0
# 环境
NVM_DIR = /home/michael/.nvm
# 默认
LEX.l = $(LEX) $(LFLAGS) -t
# 默认
LEX.m = $(LEX) $(LFLAGS) -t
# 自动
+D = $(patsubst %/,%,$(dir $+))
# 默认
COMPILE.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -c
# 环境
GDK_BACKEND = x11
# 环境
LC_PAPER = zh_CN.UTF-8
# 默认
M2C = m2c
# 环境
LC_IDENTIFICATION = zh_CN.UTF-8
# 环境
QT_ACCESSIBILITY = 1
# 环境
GIT_ASKPASS = /usr/share/code/resources/app/extensions/git/dist/askpass.sh
# 默认
MAKEFILES := 
# 自动
<F = $(notdir $<)
# 默认
CXX = g++
# 环境
LC_MEASUREMENT = zh_CN.UTF-8
# 默认
COFLAGS = 
# 默认
LINK.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
COMPILE.C = $(COMPILE.cc)
# 环境
GJS_DEBUG_TOPICS = JS ERROR;JS LOG
# 自动
^F = $(notdir $^)
# 默认
COMPILE.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(TARGET_MACH) -c
# 环境
Dotnet_Dir = /home/michael/.dotnet/tools
# 默认
SUFFIXES := .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# 默认
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# 环境
CHROME_DESKTOP = code-url-handler.desktop
# 默认
LINK.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 环境
WINDOWPATH = 2
# 默认
COMPILE.s = $(AS) $(ASFLAGS) $(TARGET_MACH)
# 默认
.INCLUDE_DIRS = /usr/include /usr/local/include /usr/include
# 环境
GJS_DEBUG_OUTPUT = stderr
# 默认
.RECIPEPREFIX := 
# 默认
MAKEINFO = makeinfo
# 默认
MAKE_TERMERR := /dev/pts/2
# 默认
OBJC = cc
# 环境
LC_ADDRESS = zh_CN.UTF-8
# 默认
LINK.f = $(FC) $(FFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# 默认
TEX = tex
# 环境
GIO_LAUNCHED_DESKTOP_FILE = /usr/share/applications/code.desktop
# 环境
LANG = zh_CN.UTF-8
# 环境
TERM = xterm-256color
# 默认
F77FLAGS = $(FFLAGS)
# 默认
LINK.o = $(CC) $(LDFLAGS) $(TARGET_ARCH)
# 变量的杂凑表状态:
# 负载=169/1024=17%, 重新杂凑=0, 碰撞=27/200=14%

# Pattern-specific 变量值

# 没有 pattern-specific 变量的值。

# 目录

# SCCS：无法对其进行 stat 操作。
# . (设备 2053，i-节点 6030305)： 9 文件， 57 不可能.
# RCS：无法对其进行 stat 操作。

# 9 文件， 57 不可能在 3 目录中。

# 隐含规则

%.out:

%.a:

%.ln:

%.o:

%: %.o
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.c:

%: %.c
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.ln: %.c
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

%.o: %.c
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

%.cc:

%: %.cc
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cc
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

%.C:

%: %.C
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.C
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

%.cpp:

%: %.cpp
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cpp
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

%.p:

%: %.p
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.p
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

%.f:

%: %.f
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.f
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

%.F:

%: %.F
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.F
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

%.f: %.F
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

%.m:

%: %.m
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.m
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

%.r:

%: %.r
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.r
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

%.f: %.r
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

%.y:

%.ln: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

%.c: %.y
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

%.l:

%.ln: %.l
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

%.c: %.l
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

%.r: %.l
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

%.ym:

%.m: %.ym
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

%.yl:

%.s:

%: %.s
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.s
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

%.S:

%: %.S
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.S
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

%.s: %.S
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

%.mod:

%: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

%.o: %.mod
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

%.sym:

%.def:

%.sym: %.def
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

%.h:

%.info:

%.dvi:

%.tex:

%.dvi: %.tex
#  要执行的配方 (内置)：
	$(TEX) $<

%.texinfo:

%.info: %.texinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.texi:

%.info: %.texi
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texi
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.txinfo:

%.info: %.txinfo
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.txinfo
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.w:

%.c: %.w
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

%.tex: %.w
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

%.ch:

%.web:

%.p: %.web
#  要执行的配方 (内置)：
	$(TANGLE) $<

%.tex: %.web
#  要执行的配方 (内置)：
	$(WEAVE) $<

%.sh:

%: %.sh
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

%.elc:

%.el:

(%): %
#  要执行的配方 (内置)：
	$(AR) $(ARFLAGS) $@ $<

%.out: %
#  要执行的配方 (内置)：
	@rm -f $@ 
	 cp $< $@

%.c: %.w %.ch
#  要执行的配方 (内置)：
	$(CTANGLE) $^ $@

%.tex: %.w %.ch
#  要执行的配方 (内置)：
	$(CWEAVE) $^ $@

%:: %,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%,v
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: RCS/%
#  要执行的配方 (内置)：
	$(CHECKOUT,v)

%:: s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

%:: SCCS/s.%
#  要执行的配方 (内置)：
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

# 92 条隐含规则，5 (5.4%) 终端。

# 文件

# 不是一个目标：
.web.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TANGLE) $<

# 不是一个目标：
.l.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

# 不是一个目标：
.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.ym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.f.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.f) $(OUTPUT_OPTION) $<

# 不是一个目标：
.l:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.y:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.def.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.def) -o $@ $<

.PHONY: install
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.p.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.p) $(OUTPUT_OPTION) $<

# 不是一个目标：
.p:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.txinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.a:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.yl:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

# 不是一个目标：
.F.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.F) $(OUTPUT_OPTION) $<

# 不是一个目标：
.texi.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.w.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CTANGLE) $< - $@

# 不是一个目标：
.texi.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.ch:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.m.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.m) $(OUTPUT_OPTION) $<

# 不是一个目标：
.cc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.cc.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

# 不是一个目标：
.def:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.SUFFIXES: .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.c.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.c) $(OUTPUT_OPTION) $<

# 不是一个目标：
Makefile:
#  对隐含规则的搜索已完成。
#  最近更新 2023-01-11 21:10:04.222267172
#  文件已经被更新。
#  更新成功。
#  要执行的配方 (从“Makefile”，行 7)：
	cd src && $(MAKE) $@

# 不是一个目标：
.r.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
.r:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.ym.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.m) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
.y.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

# 不是一个目标：
.elc:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.l.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.l) $< > $@

# 不是一个目标：
.out:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
all:
#  对隐含规则的搜索已完成。
#  隐含/静态模式主干：“”
#  文件不存在。
#  文件已经被更新。
#  更新成功。
# 自动
# @ := all
# 自动
# % := 
# 自动
# * := 
# 自动
# + := 
# 自动
# | := 
# 自动
# < := all
# 自动
# ^ := 
# 自动
# ? := 
# 变量的杂凑表状态:
# 负载=8/32=25%, 重新杂凑=0, 碰撞=2/14=14%
#  要执行的配方 (从“Makefile”，行 7)：
	cd src && $(MAKE) $@

# 不是一个目标：
.C:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.r.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

# 不是一个目标：
.S:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.texinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.w.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(CWEAVE) $< - $@

# 不是一个目标：
.c.ln:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINT.c) -C$* $<

# 不是一个目标：
.s.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.s) -o $@ $<

default: all
#  对隐含规则的搜索已完成。
#  文件不存在。
#  文件已经被更新。
#  更新成功。
# 变量的杂凑表状态:
# 负载=0/32=0%, 重新杂凑=0, 碰撞=0/5=0%

# 不是一个目标：
.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.texinfo.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# 不是一个目标：
.el:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.lm.m:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	@$(RM) $@ 
	 $(LEX.m) $< > $@

# 不是一个目标：
.y.c:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(YACC.y) $< 
	 mv -f y.tab.c $@

# 不是一个目标：
.web.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(WEAVE) $<

# 不是一个目标：
.texinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

.DEFAULT:
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 7)：
	cd src && $(MAKE) $@

# 不是一个目标：
.h:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.tex.dvi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(TEX) $<

# 不是一个目标：
.cpp.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

# 不是一个目标：
.cpp:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.C.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.C) $(OUTPUT_OPTION) $<

# 不是一个目标：
.texi:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.txinfo:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.tex:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.txinfo.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# 不是一个目标：
.sh:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	cat $< >$@ 
	 chmod a+x $@

# 不是一个目标：
.S.s:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.S) $< > $@

# 不是一个目标：
.mod:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ -e $@ $^

# 不是一个目标：
.mod.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.mod) -o $@ $<

# 不是一个目标：
.F.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

# 不是一个目标：
.w:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.S.o:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(COMPILE.S) -o $@ $<

# 不是一个目标：
.F:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.web:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.sym:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

# 不是一个目标：
.f:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。
#  要执行的配方 (内置)：
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

# 不是一个目标：
.info:
#  内建规则
#  对隐含规则的搜索尚未完成。
#  从不检查修改时间。
#  文件尚未被更新。

install:
#  假目标 (.PHONY的前提)。
#  对隐含规则的搜索尚未完成。
#  文件不存在。
#  文件尚未被更新。
#  要执行的配方 (从“Makefile”，行 10)：
	cd src && $(MAKE) $@

# 文件杂凑表状态:
# 负载=77/1024=8%, 重新杂凑=0, 碰撞=490/1793=27%
# VPATH 搜索路径

# 没有“vpath”搜索路径。

# 没有通用搜索路径(“VPATH”变量)。

# strcache 缓冲: 1 (0) / 字串 = 487 / 空间 = 5349 B / 平均 = 10 B
# 目前缓冲: 尺寸 = 8162 B / 已用 = 5349 B / 数量 = 487 / 平均 = 10 B

# strcache 性能：查找 = 825 / 命中率 = 40%
# 杂凑表统计数据:
# 负载=487/8192=6%, 重新杂凑=0, 碰撞=230/825=28%
# 在 Wed Mar 15 10:42:52 2023
 上完成 make 数据基础
