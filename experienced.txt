20220118
1 没想到mv到一个非目录居然成功了,但是文件全部丢失了
2 gcc编译c语言时候有个bug,生命变量时候为0但是不生效,只有强制给赋值才可以!!!
3 应该建立一个命令集成,方便返回去看看之前的命令,不然终端只有有限的条数而且不智能
4 git clone 时候要可能要改https为git才行,也是比较奇怪的地方
5 git 经过验证还是ssh key比较靠谱 
6 gcc -o main main.c
7 守护进程 需要释放父进程和修改文件描述符等
8 无名通道是父子进程之间的应用,有名通道是两个不同进程的通信,且都是单向的
9 进程还可以通过信号来进行通信.比较复杂,类似windows的消息队列消息泵等.有一些
  常用的常量,当捕捉到键盘信息时候可以交给注册的函数指针指向函数来进行处理,且
  非阻塞,应用较广.
  Think:与事件注册有何不同和联系呢?
10 利用IPC队列进行通信,实际上跟通道差不多,都是需要两个标志或者说文件之类.
