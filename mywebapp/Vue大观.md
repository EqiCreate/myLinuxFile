Vue：

1 组件化  component

2 自定义指令和灵活的 语法糖如 :href ,@click ,.prevent，v-if,v-for

3 mvvm 模式，双向绑定，注重业务逻辑

4 计算属性和属性computed,method,内存占用更小

5 灵活 样式绑定方式

6 父子组件之间必须&emit，进行调用，减少内存泄漏

7 定义路由机制

8 简单的 transition和搭配css的动画特效和过渡效果

9 注入式组件开发，可支持混入，合并等逻辑

10 http方面需要结合axios

11 响应式接口&watch，但是不能检测到对象属性的添加或删除，最好的方式就是在初始化实例前声明根级响应式属性，哪怕只是一个空值。

12 强大结合devtools工具进行调试。